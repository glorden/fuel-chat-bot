# Прогресс

Статус этапов. Дизайн — в [ARCHITECTURE.md](ARCHITECTURE.md).

- [x] **Этап 0 — Инициализация репозитория** (2026-08-08): git-репозиторий, `.gitignore`, README/CLAUDE/ARCHITECTURE/PROGRESS, `requirements.txt`, `.env.example`, подключён и запушен GitHub — https://github.com/glorden/fuel-chat-bot.
- [x] **Этап 1 — VK-пламбинг** (2026-08-08): `bot.py`/`config.py`/`vk_handlers.py` — Long Poll на vkbottle, фильтр собственных сообщений бота. Сообщество «Свидетель Бензовоза» (club240747054), токену нужны права **«Управление сообществом» + «Сообщения сообщества»** (только «Сообщения» даёт ошибку 15/1133 на `groups.getLongPollServer`). Проверено на реальном сообщении из беседы (`peer_id=2000000001`) — дошло и залогировалось. Заодно добавлен `loguru` в зависимости — без него vkbottle падает с невнятной ошибкой при любом сбое вместо нормального лога.
- [x] **Этап 2 — Тихий сбор данных** (2026-08-08): `gazetteer.yaml` (7 станций), `pipeline/{prefilter,extract,resolve_station,pipeline}.py`, `db/{schema,repo}.py`, `vk_handlers.py` теперь пишет в БД (`fuel_report`/`unresolved_mention`), ответов в чат по-прежнему нет. Проверено на 15 реальных сообщениях беседы (`tests/fixtures.py` + `tests/test_pipeline.py`, все зелёные) — не на синтетике. По ходу нашлось и поправлено:
  - Пре-фильтр пропускал не все реальные вопросы («У суоярвского или другая?» — без ключевых слов темы, только «?»); раз беседа целиком тематическая, теперь любое «?» тоже пропускается дальше.
  - Бренд «Роснефть»/«РН» в переписке — минимум 3 разные точки города (Лыжная, Лесной 79/въезд в город, Комсомольский — последняя не подтверждена). Голых алиасов «рн»/«роснефть» без адреса в газетире нет осознанно — иначе резолвер их путает.
  - Пользователь подтвердил и дополнил газетир: РН на Комсомольском = РН у Ленты (новая точка `rosneft_komsomolsky`); Татнефть/Лукойл/Опти на Шуйском шоссе — три разные станции трёх разных брендов на одной улице (`tatneft_shuyskoe`, `lukoil_shuyskoe`, `opti_shuyskoe`); «суоярвский» и «на объездной» — локальные названия той же станции, что и «Лесной 79» (алиасы добавлены туда же, не новая станция). Газетир вырос с 7 до 11 станций. Резолвер переработан на матчинг «бренд + локация» раздельно (плюс резолв по одной уникальной локации без бренда) вместо жёстких фраз — надёжнее с учётом обнаруженных пересечений брендов/улиц. `rapidfuzz` из зависимостей убран как более не нужный.
  - Янишполе (посёлок, не конкретная АЗС) в газетир не добавлен — по-прежнему уходит в `unresolved_mention`.
  - Прогнали больший реальный лог (1-8 августа, ~40 сообщений). Нашли и починили:
    - «Скиньте инфу когда появится 95 в Вилге» уходило в отчёт («95 есть в Вилге») вместо вопроса — не было слова «когда» среди маркеров вопроса. Добавлено.
    - Склонения брендов ломали и пре-фильтр, и резолвер: «Роснефти»/«Татнефти» не матчились на `роснефть\w*`/`татнефть\w*`, потому что при склонении мягкий знак пропадает («нефть»→«нефти»), а не просто дописывается суффикс. Стем поправлен на «роснефт»/«татнефт» (без ь) везде.
    - Добавлена станция `rosneft_lososinskoe` (Роснефть, Лососинское шоссе, у РБ) — 3 независимых сообщения на неё указывали, в старом газетире её не было (только Лукойл на Лососинском).
    - Реальная опечатка «лоссосинское» (двойное с) добавлена отдельным алиасом, а не через fuzzy-matching — проще и предсказуемее.
  - «Опти на суоярвском» — уточнили у пользователя: ошибка спрашивающего, станция одна («опти шуйское»), газетир не трогали.
  - Известное ограничение: нет учёта reply/thread-связей между сообщениями, короткие уточнения без ключевых слов темы («Лесной 79» отдельным сообщением) не подхватываются. Задокументировано в ARCHITECTURE.md, в MVP не чинится (частично компенсировано в Этапе 3 — см. ниже).
  - Ещё один реальный лог (~150 сообщений). Газетир вырос с 11 до 14 станций: `tatneft_obiezdnaya` и `lukoil_zavodskaya` подтверждены по 2 независимым сообщениям каждая; `opti_kazarmenskaya` — пользователь подтвердил, что это отдельная вторая точка Опти (не то же самое, что «опти шуйское» — из-за этого голое упоминание «опти» без адреса больше не резолвится однозначно, как и у остальных многоточечных брендов). «Роснефть на высотной» упомянута один раз — решили подождать ещё подтверждений, в газетир не добавляли. Плюс: алиас «больниц» к `rosneft_lososinskoe` (люди пишут и «РБ», и полностью «Республиканская больница»); очередь в машинах («6 машин», «3-4 машины») — раньше ловился только формат в минутах.
  - Новый класс пробела, подтверждённый несколько раз («всё есть», «всё появилось», «бензина не было, только дизель»): сообщение явно про наличие, но без конкретного числа марки — regex структурно не извлекает факт (нет цифрового токена). Живая проверка через `llm/client.py` показала, что LLM-путь такие сообщения разбирает верно (разворачивает «всё» в статус по каждой отслеживаемой марке) — сознательно не патчим regex под это, сложно сделать безопасно. Задокументировано и покрыто тестом-маркером (`test_generic_availability_without_explicit_grade_is_a_known_gap`).
- [x] **Этап 3 — MVP вопрос-ответ** (2026-08-08): `pipeline/qa.py`, `pipeline/facts.py`, `templates.py` (свежо/устарело/старо/нет данных/не распознана станция/конфликт), оба режима триггера (`AUTO_REPLY_ON_QUESTION`, по умолчанию `false`), дебаунс на чат. Юнит-тесты (`tests/test_qa.py`) все зелёные. Живой прогон в реальной беседе:
  - ✅ Упоминание бота (`message.is_mentioned`) → резолв станции → ответ реплаем — подтверждено на реальных вопросах несколько раз.
  - ✅ Ответ «нет данных» и «не распознана станция» — подтверждено вживую.
  - ✅ Резолв станции без явного бренда, только по локации (например «есть бенз 92 в вилге?» без слова «Лукойл») — подтверждено вживую.
  - ⚠️ Найден и починен реальный баг: `bot.labeler.message_view.replace_mention` по умолчанию `False` в vkbottle — без явного включения `is_mentioned` молчит без ошибок. См. CLAUDE.md/ARCHITECTURE.md.
  - ⚠️ Найден и починен реальный сценарий: тег бота реплаем на чужой вопрос без своего текста — добавлен фолбэк на текст `reply_message`. Код задеплоен, но именно этот сценарий (пустой текст + реплай + упоминание) в повторном тесте пользователь не воспроизвёл буквально — подтверждён логикой и юнит-строительными блоками (`extract`/`resolve_station`/`answer_question` по отдельности протестированы), но не финальным сквозным живым прогоном именно этой комбинации.
  - ⚠️ Найдено, не починено: резолвер матчит бренд по точному стему — опечатка в самом бренде («газпропе» вместо «газпроме») ломает резолв, бот отвечает «не распознана станция» вместо реального ответа. В отличие от опечаток в локациях (чиним точечными алиасами), это про бренды — пока не обсуждали, стоит ли чинить тем же способом.
  - По прямому запросу пользователя пороги свежести откалиброваны под этот чат: `FRESH_MINUTES=240` (4 часа), `STALE_MINUTES=480` (8 часов) — обстановка на АЗС держится часами, не минутами.
  - Не проверено вживую (только юнит-тестами с синтетическими таймстампами): устаревшие/старые данные, конфликтующие отчёты, `AUTO_REPLY_ON_QUESTION=true`, дебаунс под реальной нагрузкой.
- [x] **Этап 4 — LLM-анализ поверх rule-based (Groq)** (2026-08-08): `llm/client.py` (tool calling, валидация ответа, откат на rule-based при любом сбое), `llm/prompts.py` (промпт + полный газетир с алиасами + few-shot), `pipeline/pipeline.py` теперь пробует LLM первым при `LLM_ENABLED` (по умолчанию `false`) — rule-based код не удалён, это фолбэк. 9 новых юнит-тестов (валидация ответа Groq, wiring фолбэка через замоканный клиент) — все зелёные, 25/25 суммарно.
  - Живое сравнение на реальном Groq (`python -m scripts.compare_llm_vs_rules`, те же сообщения, что в `tests/fixtures.py`): 23 из 30 совпали с rule-based один в один.
  - По ходу сравнения нашли и починили баг в самом промпте: газетир для LLM не включал алиасы станций (только id/название/бренд/адрес) — из-за этого LLM не резолвила «У суоярвского или другая?», хотя rule-based резолвит. Добавили алиасы в промпт, перепроверили на реальном API — исправилось.
  - Там же уточнили правило про порядок слов при отрицании («92 нет очереди» — это про очередь, а не про 92) — LLM один раз перепутала, добавили явный пример в промпт, перепроверили — исправилось.
  - Оставшиеся расхождения (не чиним сейчас): LLM спутала составной вопрос («Есть ещё 95... и нет ли перерыва?») с отчётом; один пограничный случай резолва станции, где LLM осторожнее rule-based (оба поведения защитимы); LLM иногда находит факт про очередь без марки топлива, для которого в схеме `FuelReport` просто нет места — молча пропадает, это ограничение схемы, не бага конкретно LLM-пути. Всё задокументировано в ARCHITECTURE.md.
  - Заодно нашли и починили отдельный баг: скрипты в `scripts/` при запуске напрямую (`python scripts/имя.py`) не видели `pipeline`/`db`/`llm` — Python кладёт в sys.path папку скрипта, а не корень проекта. Правильный запуск — `python -m scripts.имя`, задокументировано в CLAUDE.md.
  - `LLM_ENABLED=false` по умолчанию — включать после того, как решим, что делать с оставшимися расхождениями.
  - Доп. живая проверка на новом логе (~150 сообщений, см. Этап 2) подтвердила главный довод в пользу LLM-пути: на «Татнефть на Шуйском всё есть...» и «бензина не было, только дизель» LLM правильно разворачивает общую фразу в статус по всем отслеживаемым маркам, а rule-based структурно не может (нет числового токена) — без каких-либо дополнительных правок промпта. Составное отрицание «92-95 нет» LLM тоже разобрала верно.
- [x] **Этап 5 — Включение LLM в проде и живой тест в обеих беседах** (2026-08-10): по решению пользователя `LLM_ENABLED=true` включён для живого теста, не только офлайн-сравнения. Полная хронология и разбор рисков — в ARCHITECTURE.md («LLM-анализ» → «Включение в проде и живой тест», «Возможное развитие»); здесь — сжато, что сделано и решено.
  - **До включения** аудит кода нашёл реальную дыру: LLM мог вернуть несуществующий `station_id`, и это не ловилось как «сбой» — на вопросе давало необработанный `KeyError` (тихая потеря ответа конкретному пользователю), на отчёте — тихий мусор в `fuel_report`. Починено: `pipeline/resolve_station.is_known_station()` + проверка в `llm/client.py::_parse_arguments`, неизвестный id → `None`, как у rule-based. Добавлен тест (`tests/test_llm_client.py`). Заодно добавлено логирование `path=llm`/`path=rule_based reason=llm_fallback` в `pipeline.py` (раньше нельзя было понять по логу, какой путь обработал сообщение) и починен `sys.stderr.reconfigure(encoding="utf-8")` в `bot.py` (кириллица в логе была нечитаема при пайпе через `grep` — `stdout` был перекодирован раньше, `stderr`, куда реально пишет `logging`, нет).
  - **Живой тест, критическая находка**: на реальном сообщении про «слив топлива... ГН напротив РБ 92,95, ДТ...» LLM в 4 из 6 идентичных повторных вызовов **придумал факт «АИ-100 недоступен»**, которого в тексте не было в принципе — не сбой, ответ формально валиден, `is_known_station` эту категорию не ловит (сфабрикован не id, а запись в `reports`). Отдельно на «Татнефть... 7 машин» (без локации, у бренда 4 точки) LLM уверенно выбрал `tatneft_shuyskoe` без всяких оснований в тексте; на другом сообщении с тремя брендами сразу, наоборот, корректно вернул `station_id=None` — поведение на неоднозначности не детерминировано. Пользователю прямо показаны цифры (не гипотеза) — решение: продолжать жить с этим риском осознанно, не выключать и не патчить точечно.
  - **Groq free tier**: дневной лимит (100k токенов на организацию, не на ключ — подтверждено текстом ошибки) исчерпался дважды за первый час теста при обычной активности чата; восстановление похоже на скользящее окно, а не разовый сброс раз в сутки. Решение пользователя: оставить как есть на сегодня, автоматический откат на rule-based и так работает чисто (проверено вживую на реальном `RateLimitError`, не только моком).
  - **Газетир пополнен по прямым подтверждениям пользователя в чате**: адрес и алиас «гн» для `gazprom` (напротив РБ/Роснефти на Лососинском); ~10 новых алиасов локаций по 6 станциям (автофорум/радиозавод/объездная у lukoil_lesnoy; цск/чистая у lukoil_lososinskoe; заводская у tatneft_shuyskoe; ломоносова у rosneft_lyzhnaya; боровая у rosneft_lesnoy_79; репникова у tatneft_klyuchevaya; правда/магнит у opti_kazarmenskaya); два слияния станций, ранее заведённых как разные, а оказавшихся одной АЗС — `tatneft_obiezdnaya` → в `tatneft_silikatny` (она же «Сулажгора»), `lukoil_zavodskaya` → в `lukoil_shuyskoe`. `tests/test_pipeline.py` поправлен под новые id. Отдельно, точечно (не по всему газетиру — см. ниже почему) в `pipeline/prefilter.py` добавлено голое «вилга» как ключевое слово темы: там всего одна АЗС, люди в чате зовут её без бренда, а прежний префильтр такие сообщения полностью терял (ни бренда, ни цифры, ни «?» — «Вилга, слив закончили, все есть кроме сотого» уходило в `off_topic`, даже не доходя до анализа).
    - Осознанно НЕ стали добавлять в префильтр все локации из газетира разом: часть новых алиасов («магнит», «правда») — обычные бытовые слова, широкое включение резко увеличило бы долю сообщений не по теме, уходящих на LLM, и так уже дефицитный дневной лимит сгорал бы быстрее.
  - **Продуктовое решение по двум беседам**: пользователь подключил бота во вторую беседу (`peer_id=2000000002`) и подтвердил — факты из обеих бесед должны использоваться совместно при ответе на любой вопрос. Код уже это делал (`_latest_facts` никогда не фильтровал по `peer_id`) — открытый вопрос из истории проекта закрыт без изменений в коде, только явным решением.
  - **Изменения в поведении ответов** (по прямому запросу пользователя): если станция не распознана ИЛИ распознана, но по ней нет ни одного отчёта — бот теперь молчит, а не пишет шаблонное «не понял, о какой АЗС» / «нет данных». `templates.render_unknown_station` удалена как ставшая мёртвым кодом. При реальном ответе бот теперь тегает автора вопроса — `[id<from_id>|Имя]` в начале сообщения, имя запрашивается через `users.get`. Заголовок ответа переформулирован: «По «X»:» → «информация по X.».
  - Известное, не задокументированное раньше ограничение архитектуры подтвердилось дважды на реальных сообщениях: и LLM, и rule-based держат только один `station_id` на сообщение — реальные сообщения про несколько станций сразу («РН у РБ... ГН напротив РБ...», «Шуйское: опти, лукойл, татнефть...») либо теряют часть фактов, либо (при неудаче) целиком уходят в `unresolved`. Не чинили — потребует смены формы `ExtractResult` на список станций.
  - Обнаружено, не исправлено: у второй беседы бот не показывается в списке участников и не тегается — судя по всему, платформенная (VK) настройка сообщества/чата, а не баг в коде (сообщения из этой беседы бот получает и обрабатывает нормально через Long Poll). Не расследовано до конца в рамках этой сессии.
  - Обнаружено, не исправлено: rule-based-отрицание («95 уже ВСЁ» = марка закончилась) не ловится — список слов-отрицаний в `extract.py` проверяется только ДО номера марки и не включает разговорное «всё» в значении «кончилось». В этот раз безвредно (станция не была названа, ушло в `unresolved`), но при названной станции дало бы неверный факт «доступно» вместо «недоступно».
  - **Живой инцидент, важная находка про свежесть**: реальная жалоба в чате — человек поехал на `tatneft_silikatny` по информации бота, приехал — а там пусто кроме ДТ/92. Разбор истории отчётов показал: не галлюцинация и не баг — 10 минутами ранее там реально был 95-й (с очередью 15 машин), и за эти 10 минут закончился. `FRESH_MINUTES=240` (4 часа) считает 10-минутный отчёт полностью свежим без всякой оговорки — калибровка не учитывает, что при большой активной очереди обстановка может меняться не за часы, а за минуты. Не чинили — открытый вопрос на будущее: учитывать ли размер очереди при расчёте доверия к свежести, не только время с момента отчёта.
  - Найден и починен реальный баг: третья точка Роснефти (`rosneft_lesnoy_40`, она же 38, у РБ) — отдельная от `rosneft_lesnoy_79` и `rosneft_lososinskoe`, подтверждена пользователем. `rosneft_lososinskoe` дополнена алиасом «высотка» (закрыло старое неподтверждённое «Роснефть на высотной»). Резолвится `rosneft_lesnoy_40` только по голым «40»/«38» без слова «лесной» рядом — реальное сообщение «на лесном 40» rule-based по-прежнему не резолвит (слово «лесной» само по себе всегда co-матчит и 79-ю точку), LLM с тем же текстом справляется по смыслу адреса.
  - «суоярвск» оказался общим алиасом сразу у `rosneft_lesnoy_79` и `tatneft_silikatny` (подтверждено пользователем живьём — «Татнефть суоярвское шоссе» это реально Сулажгора/объездная, не ошибка). Голое «на суоярвском» без бренда стало неоднозначным между двумя станциями — как и «лесн»/«рб» раньше.
  - Добавлены две станции вне Петрозаводска (подтверждено пользователем): Лукойл в Кондопоге/Янишполе (одна станция, оба названия) — старый пример в промпте «Янишполе не в списке» обновлён; и вторая точка Газпрома в Березовке. Вторая точка Газпрома сломала бы старое правило «бренд с одной точкой резолвится всегда» — по прямому требованию пользователя («голое „Газпром" — однозначно Петрозаводск») резолвер получил новую опциональную способность `default_for_brand` (см. ARCHITECTURE.md): станция-умолчание для бренда, когда ни одна его точка не подошла по локации. Флаг прокинут и в промпт LLM через список станций, отдельной логики для LLM-пути не потребовалось.
  - Найден и починен отдельный баг префильтра: цифровая ветка паттерна (`9[258]|100`) — единственная без `\w*` в отличие от всех остальных веток, из-за чего «95й»/«98е» (без дефиса) не матчили вообще («92-й» с дефисом работал только случайно). Реальное сообщение с этим паттерном молча уходило в `off_topic`.
  - Живой тест продолжался на `LLM_PROVIDER=gemini` (Groq весь день оставался в лимите). На конец сессии: 35/35 тестов, всё закоммичено и запушено, бот остановлен (деплоя по-прежнему нет).
- [x] **Этап 6 — Gemini как второй, переключаемый LLM-провайдер** (2026-08-10): причина — дневной бюджет Groq (100k токенов) исчерпывается примерно за час активного чата (Этап 5). `LLM_PROVIDER` (`groq` по умолчанию, либо `gemini`) в `config.py`. Рефакторинг: `llm/schema.py` (провайдер-агностичное ядро — `LLMAnalysis`, `_parse_arguments`, схема полей, перенесено без изменения логики), `llm/groq_client.py` (механический перенос прежней Groq-логики), `llm/gemini_client.py` (новый, SDK `google-genai`, forced function calling через `types.ToolConfig(mode="ANY")`, конвертер JSON Schema → нативный диалект Gemini — `_to_gemini_schema`, nullable как отдельный флаг вместо union-типа), `llm/client.py` — тонкий диспетчер, лениво импортирует нужного провайдера. Существующие тесты (`test_llm_client.py`, `test_pipeline_llm_fallback.py`) и `scripts/compare_llm_vs_rules.py` не потребовали изменений — оба реэкспортируют/используют только публичный `llm.client.analyze`. Новый тест `tests/test_gemini_schema.py` на конвертер схемы. 32/32 теста зелёные.
  - **RPM — узкое место, другой природы, чем у Groq.** Проверено эмпирически на реальном ключе: модель по алиасу `gemini-flash-latest` (сейчас резолвится в `gemini-3.6-flash`) — 5 запросов/минуту на бесплатном тарифе; `gemini-flash-lite-latest` — заметно лучше, ~8-9/мин, на неё и переключили дефолт `GEMINI_MODEL`. `gemini-2.5-flash-lite` — недоступна новым пользователям (404), не лимит, отдельная причина. У Groq риск «бюджет кончается за час», у Gemini — «фолбэк на rule-based может срабатывать при любом всплеске в несколько сообщений за минуту»; для тихого сбора (анализируется каждое тематическое сообщение) это потенциально хуже именно в моменты высокой активности чата — не проверено вживую в реальной беседе, только прямой нагрузкой на API.
  - **Промпт (`llm/prompts.py`) написан и живо провалидирован под Groq, на Gemini не переносится 1-в-1.** На том же наборе, что офлайн-сравнение Groq (`compare_llm_vs_rules.py`): 12 чистых совпадений из 15 (дальше — упор в RPM), 3 реальных расхождения — отчёт про Янишполе целиком потерян (ушёл в `irrelevant`), «92 нет очереди» распознано как «92 недоступен» (та самая ловушка порядка слов, для которой в промпте есть явное правило — сработавшее у Groq, не у Gemini), не резолвнулся алиас «суоярвского» без бренда (кейс, специально чинившийся в промпте для Groq в Этапе 4). Промпт под Gemini отдельно не дорабатывали — осознанно отложено по решению пользователя, `LLM_PROVIDER` остаётся `groq` по умолчанию.
  - **На главный риск, найденный в Этапе 5 (галлюцинация фактов), — обнадёживающий, но маленький сигнал.** Те же два «тяжёлых» сообщения, что вскрыли проблемы у Groq, прогнаны по 3 раза на Gemini: «слив топлива... ГН напротив РБ» — 0 из 3 придуманных марок (у Groq 4 из 6), причём станция резолвнулась осмысленнее (`gazprom`, откуда реально цифры, а не `rosneft_lososinskoe` по совпадению алиаса «рб»); «Татнефть... 7 машин» без локации — `station_id=None` во всех 3 повторах, ни разу не угадал (у Groq — уверенно и безосновательно `tatneft_shuyskoe`). 3 повтора — не статистика, только направление.
  - Живой тест в реальном чате не проводился — решили сначала закоммитить рабочую интеграцию как есть, доводку промпта и живой тест отложить.
  - **Доводка промпта (в тот же день, следующим заходом):** 3 расхождения из живой проверки выше — все починены и переподтверждены (3/3 чисто на каждое): отчёт про Янишполе больше не теряется целиком в `irrelevant` (уточнено правило — факт о марке есть, значит report, даже если станция не резолвится); «92 нет очереди на остальных колонках» больше не путается с «92 недоступен» (усилено правило + новый пример с такой же структурой фразы); «У суоярвского или другая?» снова резолвится (добавлено правило+пример на резолв по одной уникальной локации без бренда). Побочный эффект правки про локацию без бренда: «опти на суоярвском...» (бренд явно назван) стал иногда резолвиться в чужую станцию по совпадению локации — пойман в той же сессии, добавлено уточнение «явный бренд всегда главнее», после этого 2 из 3 (было 0 из 3). Полное сравнение на Gemini выросло до 15/16 чистых совпадений (было 12/15). Промпт общий с Groq — регресс-тест на Groq не проведён (дневной лимit ещё не обновился, ~16ч цикл), изменения по характеру аддитивные/уточняющие, не сужающие уже работавшие правила, но это рассуждение, не подтверждённый факт.
- [x] **Этап 7 — requirements.txt: точные версии** (2026-08-10): все зависимости зафиксированы на версии, реально стоящие в `.venv` (`vkbottle==4.10.0`, `loguru==0.7.3`, `PyYAML==6.0.3`, `python-dotenv==1.2.2`, `pytest==9.1.1`, `groq==1.6.0`, `google-genai==2.17.0`) — раньше все были на `>=`, для бота, который должен работать 24/7, это риск незамеченного breaking-апгрейда, особенно для `vkbottle`/`groq`/`google-genai`. 35/35 тестов зелёные. Первый этап из большого плана «контекст между сообщениями одного автора + репосты/реплаи VK + технические перерывы на АЗС» (плюс это и компактный формат ответа — два попутных пункта, всплывших при проработке плана) — план целиком утверждён пользователем, выполняется по одному этапу за раз с остановкой после каждого, как того требует раздел «Рабочий процесс» в CLAUDE.md. Отдельно отмечено: прогон `pytest` через Git Bash на этой машине искажает кириллицу в выводе и даёт ложный `AssertionError` на сравнении русских строк — сама логика не затронута, тесты реально зелёные при прогоне через PowerShell с `PYTHONIOENCODING=utf-8`.
- [x] **Этап 8 — templates.py: компактный формат ответа** (2026-08-10): по прямому запросу пользователя старый формат (строка на марку топлива, на каждой свой «(данные N мин назад{, оговорка})») заменён на компактный — марки с одинаковым статусом сгруппированы в одну строку («92, 95, 98, ДТ - Есть» / «100 - Нет»), один абсолютный timestamp в заголовке («на 18:40») вместо относительного возраста на каждой строке. Оговорки о неполной свежести/конфликте/очереди не потеряны — просто короче («устарело», «данные старые», «было иначе» вместо длинных фраз) и показываются на той конкретной группе марок, к которой относятся, а не размывают компактность общего случая. `StationFact` (`pipeline/facts.py`) получил поле `reported_at` (абсолютное время) в дополнение к `age_minutes` — нужно было для заголовка, вычислять его через второй независимый `datetime.now()` в `templates.py` не стали, чтобы не было дрейфа между вызовами. `MOSCOW_TZ` теперь тоже там, общий для будущих этапов. Если у показанных фактов разные пометки об очереди — каждая показывается при своей группе марок, не теряется молча. Тесты обновлены под новые формулировки + добавлены новые (группировка, время из самого свежего факта, разные очереди по маркам) — 38/38 зелёных. Живой вопрос в реальном чате не гонялся (бот не запущен, а поднимать его специально ради визуальной проверки — отдельное решение с реальными последствиями в живой беседе) — вместо этого проверено прямым локальным рендером `render_station_answer()` на синтетических данных, вывод совпал с примером пользователя один в один.
- [x] **Этап 9 — VK: репосты и реплаи с собственным текстом** (2026-08-10): `fwd_messages` раньше не читался вообще, `reply_message` — только в одном узком случае (тег бота, пустой свой текст). Теперь `vk_handlers.py::_quoted_context_text` подклеивает текст `fwd_messages`+`reply_message` (один уровень, без рекурсии) впереди собственного текста автора перед вызовом пайплайна — реплай «а очередь есть?» на чужой отчёт резолвит станцию из цитаты, а форвард без комментария читается как отчёт, который он несёт. `process_message()` получил kwarg `own_text` (по умолчанию — `text`, старые вызовы не затронуты) — используется только гвардом: если факт всплыл лишь из подклеенной цитаты, а свой текст автора сам по себе ничего не сообщает (например «спасибо»-реплай), запись в БД подавляется — иначе случайный реплай искусственно освежил бы старый факт под новым timestamp. `_answer_for_parent_message` убрана целиком — её случай (упоминание + пустой текст + реплай) теперь покрывается общим путём, и заодно это чинит то, чего старая функция не умела: реплай без текста на **отчёт** (не только на вопрос) тоже подхватывается. По ходу отладки тестов нашлась и починена реальная ошибка в самом гварде (перепутанное условие — суппрессия срабатывала ровно наоборот, на пустом own_text вместо непустого) — поймано юнит-тестами сразу, не попало в коммит. 47/47 тестов зелёные (5 новых на `_quoted_context_text`, 4 новых на гвард в `process_message`). Живой тест в реальном чате не гонялся (бот не запущен). Отдельно замечено, не чинилось: `.env` на этой машине держит `LLM_ENABLED=true` с обоими API-ключами настоящими — это значит, что уже существовавший `test_process_message_end_to_end_against_temp_db` в `test_pipeline.py` неявно бьёт в живой Gemini при каждом прогоне тестов (а не только rule-based), а не через мок; новые тесты этого этапа сознательно форсируют `LLM_ENABLED=False` через `monkeypatch`, чтобы не зависеть от живого ответа модели. Стоит когда-нибудь поправить и старый тест так же, но это не входило в объём этого этапа.
- [x] **Этап 10 — контекст между сообщениями одного автора (только LLM-путь)** (2026-08-10): сценарий «95 есть на Газпроме?» → следом «большая очередь?» без станции — второе сообщение раньше не резолвилось. `vk_handlers.py` теперь держит короткоживущий in-memory кэш последнего тематического сообщения каждого автора (`_last_author_message`, ключ `(peer_id, author_id)` — не объединяется между двумя подключёнными беседами, в отличие от фактов; TTL 120 сек, не откалиброван) и прокидывает его как `previous_message` через `process_message` → `_analyze` → `llm.client.analyze` → `raw_analyze` обоих провайдеров. Только LLM-ветка его получает — `extract()` (rule-based) не тронут, у него нет механизма это использовать. Тело запроса к модели теперь строит общий `llm/prompts.py::build_user_content` (первый случай, когда Groq- и Gemini-клиенты используют общий хелпер для тела запроса, а не строят его каждый по-своему) — с явной разметкой «предыдущее только для контекста, разобрать нужно текущее», плюс новое правило и пример в системном промпте. 53/53 теста зелёные (5 новых). **Живой тест против настоящего API (не через сам бот — бот не поднимался) на обоих провайдерах**: с контекстом `station_id=gazprom` резолвится верно на Gemini И на Groq (лимит на момент проверки оказался свободен); без контекста то же самое сообщение даёт `station_id=None` — то есть эффект именно от контекста, не совпадение/угадывание.
- [x] **Этап 11 — технические перерывы на АЗС, rule-based путь** (2026-08-10): новая append-only таблица `station_break` (`station_id`, `kind`, `until`, `duration_note` + обычные поля происхождения) — пишется в `pipeline.py` наряду с `fuel_report`, не вместо него. `pipeline/extract.py` реструктурирован: перерыв — независимый от `message_type` сигнал (можно быть и в отчёте, и в вопросе), не 4-е значение enum'а. Раньше чистое сообщение о перерыве без марки топлива проваливалось в `irrelevant`, из-за чего `resolve_station()` для него вообще не вызывался (`_analyze` пропускает резолв только для `irrelevant`) — теперь условие `reports or break_info is not None` эскалирует его в `report` с пустым `reports`, станция резолвится. Вопросы `break_info` никогда не несут — ответ про активный перерыв всегда строится из БД в `qa.py`, не из текста текущего вопроса. Новый `resolve_clock_time_to_iso()` валидирует диапазон часов/минут и возвращает `None` на мусоре вместо падения — понадобилось не для перестраховки: паттерн диапазона времени («с 21-22 ч») без этой защиты и без обязательного суффикса "ч"/"час" совпал бы и с обычным диапазоном марок топлива («92-95»), дав `hour=92` и `ValueError` в `datetime.replace()` — поймано и закрыто тестом `test_grade_range_is_not_mistaken_for_a_time_range` до того, как это стало реальным багом. `qa.py::_latest_break` — **по прямому решению пользователя, без окна активности**: всегда последняя запись по станции, с возрастом, без автоматического «протух через N минут» — перерыв, о завершении которого никто не написал, будет упоминаться сколь угодно долго с растущим «сообщили N назад» (осознанное следствие, не побочный эффект). «Молчание» в `answer_question` теперь требует одновременно пустых фактов по маркам И отсутствия перерыва. `templates.py` показывает перерыв и факты по маркам вместе, не одно вместо другого (тоже по решению пользователя). 67/67 тестов зелёные (14 новых); проверено, что оба существующих вопроса-фикстуры про «перерыв» не задеты (вопросы обрабатываются раньше, break-экстракция для них не запускается вовсе). Живой чат-тест не гонялся (бот не запущен) — вместо этого прогнан локальный рендер на синтетических данных (совпало с ожиданиями) и прямая проверка, что миграция схемы чисто применяется к реальному `bot.db` (`CREATE TABLE IF NOT EXISTS`, без вставки данных).
- [x] **Этап 12 — технические перерывы на АЗС, LLM-путь** (2026-08-10): последний этап большого плана. `llm/schema.py::PARAMETERS_SCHEMA` получил вложенный nullable `break_info` (`kind`/`until`/`duration_note`, без `enum` на `kind` — сочетание `nullable`+`enum` нигде в проекте ещё не проверено вживую на Gemini, остались на уже проверенном паттерне свободной nullable-строки как у `queue_note`). `_parse_break_info` зеркалит валидацию rule-based-стороны и переиспользует тот же `resolve_clock_time_to_iso`. Важное архитектурное решение: `break_info` гейтится через `message_type == "report"` в самом `_parse_arguments`, а не отдельным флагом `is_question` внутри `break_info` (был в черновом плане, убран как дублирующий и потенциально противоречащий источник истины — `message_type` и так это говорит). `llm/prompts.py` — новое правило + 4 примера: чистый перерыв без марки — всё равно `report`, а не `irrelevant` (симметрично rule-based-правке); явное «до HH:MM»; перерыв+отчёт в одном сообщении; и главное — прогноз на будущее («95 появится через час») не должен фабриковать текущий факт в `reports` (ровно та находка, с которой в ARCHITECTURE.md начиналось обсуждение галлюцинаций Groq).
  - **Живая проверка на обоих провайдерах, поштучно (не через `compare_llm_vs_rules.py` одним проходом)**: прогон скрипта целиком уткнулся в RPM-лимит Gemini (~8-9 запросов/мин) ещё до того, как дошёл до новых фикстур про перерывы, добавленных в конец списка — все они получили `LLM FAILED (None)`. Перепроверено по одному сообщению отдельными вызовами (без исчерпания лимита предыдущими вызовами в том же прогоне): **на Gemini все 4 фикстуры-перерыва совпали с rule-based один в один**, ловушка с прогнозом на будущее не фабрикует факт (`reports=[]`, `duration_note="через час"`), вопрос про перерыв корректно даёт `break_info=None`.
  - **На Groq — 5 из 6 проверок получили реальный ответ**, дневной токен-бюджет (100k) исчерпался прямо в процессе (подтверждено текстом самой ошибки: «Used 98737, Requested 4185») на последней проверке (вопрос про перерыв) — она осталась неподтверждённой на Groq, нужно перепроверить, когда лимит освободится. Из полученного: все 4 фикстуры-перерыва и ловушка с прогнозом на будущее совпали с rule-based и Gemini.
  - **Отдельно найдено, не вызвано этим этапом**: на Groq «Отстой топлива на Опти Шуйское с 21-22 ч» дало `station_id=None`, хотя и rule-based, и Gemini верно резолвят `opti_shuyskoe` — резолв станции в этом этапе не менялся вообще, укладывается в уже задокументированную в ARCHITECTURE.md картину «Groq местами менее аккуратен, чем Gemini», не новая регрессия.
  - `scripts/compare_llm_vs_rules.py` расширен — печатает `break=...`, включает `BREAK_REPORTS` в сравнение для будущих прогонов. 75/75 тестов зелёные (13 новых: разбор `break_info` включая мусорный/отсутствующий ввод, гейтинг по `message_type`, первый вложенный object-тип в конвертере схемы Gemini).
  - **На этом весь план (Этапы 7-12) завершён.**
- [x] **Этап 13 — разметка процитированного контекста для LLM** (2026-08-11): живой прогон бота (см. ниже, «Живые тесты 2026-08-11») нашёл закономерность — короткие подтверждающие реплики в треде («Да», «Есть.») на цитату с «?» повторно (4 раза за вечер) классифицировались как `question` вместо `report`. Причина: `previous_message` (Этап 10) уже был явно размечен для LLM («Предыдущее сообщение... только для контекста» / «Текущее сообщение...»), а процитированный текст реплая/форварда (Этап 9) склеивался с собственным текстом простой конкатенацией без всякой разметки — LLM не мог понять, какая часть текста «фон», а какая — то, что реально нужно разобрать. `quoted_context` теперь идёт отдельным параметром через `analyze()`/`raw_analyze()`, размечается в `build_user_content()` точно как `previous_message`. **Важно: rule-based-ветка не тронута** — `_analyze()` для неё по-прежнему получает простую склейку (`text`), не размеченную версию — регексу разметка не нужна и не мешает, а склейка ему помогает резолвить станцию. Новый пример 13 в промпте на реальном сообщении. Заодно (по ходу, раз уж трогали этот файл) починена старая известная нестабильность `test_process_message_end_to_end_against_temp_db` (не мокала `LLM_ENABLED`, ловила живой Gemini). 80/80 тестов зелёные (6 новых). **Живая проверка** на тех же реальных сообщениях, что вскрыли проблему: «Да»/цитата «А Татнефть на Шуйском есть 95?» → теперь `report`/`tatneft_shuyskoe`/95 available (было `question`); «Есть.»/цитата «95 есть на Лококсинском?» → `report`/`rosneft_lososinskoe` (было `question`).
- [x] **Этап 14 — лимит отпуска топлива на АЗС** (2026-08-11): по прямому запросу пользователя во время живого мониторинга — вопросы/факты про лимит («сколько заливают», «лимит 20 литров в одни руки») реально частые в чате, а старое правило промпта уносило их в `irrelevant` (найдено тем же вечером на реальном «Татнефть сколько заливают?»). Реализовано по образцу `station_break`: новая append-only таблица `fuel_limit` (`station_id`, `status` "limited"/"unlimited", `liters`), факт про станцию целиком, показывается вместе с фактами по маркам и перерывом при ответе на любой вопрос по станции (не только когда лимит явно спрашивают — та же логика, что уже работает для перерывов). `qa.py::_latest_limit` — тоже без окна активности, последняя запись всегда побеждает. `limit_info` в `extract.py` — независимый от `message_type` сигнал, эскалирует чистое сообщение о лимите из `irrelevant` в `report`, симметрично `break_info`. Найдена и закрыта до коммита реальная коллизия: «АИ-100» — отслеживаемая марка, а «100» — правдоподобное значение лимита; «100 л»/«100 литров» без явного исключения в regex грозило задвоением в мусорный отчёт «марка 100 доступна» — добавлена проверка на суффикс «л»/«литр» сразу после числа. `llm/schema.py` — `limit_info` гейтится через `message_type == "report"`, как и `break_info`. `llm/prompts.py` — обновлены верхнеуровневые определения report/question/irrelevant (та же категория пробела, что чинили для перерывов в Этапе 12-13, только для лимита), убрано «лимит литров» из примеров irrelevant (цена осталась — её по-прежнему не отслеживаем). 98/98 тестов зелёные (22 новых). **Живая проверка на обоих провайдерах**: 4/4 совпало с rule-based на Gemini и на Groq (дневная квота Groq за ночь обновилась) — заодно этим же прогоном закрыт хвост, оставшийся с Этапа 12 (вопрос про перерыв, не проверенный на Groq тогда из-за лимита, — теперь подтверждён верным).
- [x] **Этап 15 — Docker-деплой + SOCKS5-прокси для LLM: код и инфраструктура готовы, сам деплой на сервер — впереди** (2026-08-13): бот с Этапа 5 нигде не задеплоен. Перед тем, как писать код, вживую (по SSH) проверено, а не предположено: целевой VPS (`library-vps`) уже держит два чужих проекта на Docker Compose, ресурсы жёсткие (диск 8.7G/свободно 1.8G, память 961Mi всего) — учтено в `DEPLOY.md`. С этого VPS прямые вызовы `api.groq.com`/`generativelanguage.googleapis.com` дают `403` за <200мс (edge-блок по гео, не баг), VK API — `200` без прокси. У соседнего проекта (`book library`, тот же пользователь) уже есть рабочий приём для той же проблемы — SSH SOCKS5-туннель на внешний сервер вне России (сайдкар `ai-proxy`); его туннель проверен вживую как реально рабочий (не только «контейнер поднят»), и тем же туннелем проверено, что Groq через прокси даёт `401` (блокировка обойдена), а Gemini — всё ещё `403` (похоже, Google блокирует и сам внешний узел, не только Россию). По прямому решению пользователя: бот получает **свой независимый** `ai-proxy`-сайдкар и свой SSH-ключ (не общий с book library, общий только внешний сервер-цель), и на VPS включается **только `LLM_PROVIDER=groq`** — Gemini-через-прокси код написан симметрично (`llm/gemini_client.py`, `HttpOptions.client_args`), но не проверен живым трафиком, задокументирован как осознанный пробел.
  - Новое: `Dockerfile`, `.dockerignore`, `docker-compose.yml` (сервисы `app`+`ai-proxy`, bind-mount `bot.db`(+`-shm`/`-wal`) и `gazetteer.yaml`), `docker/ai-proxy/{Dockerfile,entrypoint.sh}` (копия проверенного приёма book library, независимая), `DEPLOY.md` (по прямому запросу пользователя — переменные окружения, ресурсы library-vps, устройство туннеля, стадийная процедура, операционные нюансы, бэкапы как открытый риск). `CLAUDE.md`/`ARCHITECTURE.md` — ссылки на `DEPLOY.md` вместо дублирования.
  - Код: `config.py` (`AI_PROXY_URL`), `llm/groq_client.py`/`llm/gemini_client.py` — при заданном `AI_PROXY_URL` клиент SDK получает `httpx.Client(proxy=...)` через штатную точку расширения (`http_client=` у Groq, `HttpOptions.client_args` у Gemini — подтверждено чтением исходников SDK, для Gemini этот путь новый, не скопирован из book library, которая проксирует только Claude/OpenAI-совместимый). Пусто/не задано — поведение не меняется, локальная разработка не затронута. `requirements.txt`: `httpx==0.28.1` (была только транзитивная, теперь оба клиента используют её напрямую) + `socksio==1.0.0` (нужен httpx для `socks5h://`, версия сверена реальной установкой в venv, не угадана).
  - 98/98 тестов зелёные без изменений (новых юнит-тестов на этом этапе нет — это инфраструктура и обвязка, не новая логика пайплайна; сам прокси-путь можно проверить только живым трафиком, не pytest).
  - **Не сделано в этом заходе**: сам деплой на сервер. Заблокировано на шаге генерации ключа туннеля — публичный ключ нужно добавить в `authorized_keys` на внешнем сервере (`217.60.252.16`), а доступа к нему в текущей сессии нет (нет SSH-алиаса). Следующий шаг — Этап 16: клон на `library-vps`, перенос `.env`/`bot.db`/`gazetteer.yaml`, генерация и прописывание ключа, первый запуск с выключенным LLM, проверка туннеля, включение `LLM_ENABLED=true`, живая проверка в реальном чате — процедура целиком расписана в `DEPLOY.md`.
- [x] **Этап 16 — деплой на library-vps и живая проверка** (2026-08-13): продолжение Этапа 15, та же сессия. Репозиторий склонирован в `/home/deploy/fuel-chat-bot`; `.env` (после `PRAGMA wal_checkpoint(TRUNCATE)` — WAL весил 1.2MB против 44KB `bot.db`, без checkpoint часть накопленных данных не переехала бы), `bot.db`, `gazetteer.yaml` перенесены `scp` с машины разработчика; пустые `bot.db-shm`/`bot.db-wal` созданы на хосте заранее, чтобы bind-mount не подставил директорию вместо файла. Ключ для отдельного туннеля бота сгенерирован на `library-vps` (не на машине разработчика); публичный ключ пользователь добавил в `authorized_keys` `tunneluser` на `217.60.252.16` вторым, независимым от book library ключом (тот же `tunneluser`, книжный проект не тронут). `known_hosts`, запиненный через `ssh-keyscan`, сверен с уже доверенным пином book library на том же хосте — ed25519-отпечаток совпал побайтово, конфигурация не подставная. Ключ проверен на аутентификацию отдельным `-N`-подключением ещё до сборки образов — чтобы не тратить и так дефицитный диск на сборку, если ключ на внешнем сервере не встанет.
  - **Первый запуск, `LLM_ENABLED=false`**: `docker compose up -d --build` чистый, бот подключился к VK (`groups.getById`/`groups.getLongPollServer` прошли), слушает Long Poll. Все 6 чужих контейнеров (`travel-taxiptz`, `home-library-catalog`) остались `Up`, не задеты. Диск 1.8G→1.4G свободных (новые образы, в рамках ожидаемых 300-450MB), бот+свой ai-proxy — суммарно ~85MB RAM (78.6+5.9).
  - **Проверка туннеля** (уже на выделенной инфраструктуре бота, не заимствованной у book library): прямой вызов `api.groq.com` — `403`, тот же вызов через `socks5h://ai-proxy:1080` — `401` — блокировка обходится собственным туннелем, не только туннелем book library, которым сверялись на этапе планирования.
  - **`LLM_ENABLED=true`, `LLM_PROVIDER=groq`** — пересоздание контейнера (`--force-recreate`, обычный `restart` не перечитал бы `.env`), старт чистый.
  - **Живой мониторинг реального трафика беседы** (не синтетика): `path=llm message_type=question station_id=rosneft_lyzhnaya` на реальном «Рн на ломоноса есть 95?» — LLM через новый туннель верно резолвнул станцию по алиасу «ломоносова», даже в усечённой форме «ломоноса». Отдельно поймана и разобрана реальная ошибка: на сообщении-шутке «У тебя FB или FA?» (вместо марки топлива) Groq вернул `400 tool_use_failed` — сама модель попыталась записать `"FB"`/`"FA"` в `question_grades`, но enum-схема Groq (только `100/92/95/98/ДТ`) отклонила вызов на уровне API, раньше чем ответ дошёл до нашей валидации в `llm/schema.py`. Откат на `path=rule_based reason=llm_fallback` сработал чисто, бот не упал — новый, ранее не встречавшийся в такой форме (отказ на стороне Groq API, не сбой сети и не наша валидация) вариант уже задокументированного класса рисков LLM-пути. Остальные живые сообщения — офф-топик (пре-фильтр отсеял до классификатора) и один `station_id=None` без резолва (сообщение вида «только что оттуда» без реплая и вне TTL кэша предыдущего сообщения — известное, задокументированное ограничение однослотового контекста, не новая находка).
  - **Итог**: деплой полностью функционален, `LLM_PROVIDER=groq` через выделенный прокси подтверждён на живом трафике беседы, а не только точечными вызовами. Открыто и осознанно отложено — Gemini через этот же туннель (см. Этап 15, DEPLOY.md) и бэкапы `bot.db` вне VPS (см. DEPLOY.md, «Бэкапы»).
- [x] **Этап 17 — третий LLM-провайдер: Mistral (код, деплой на VPS не сделан)** (2026-08-13): прямое следствие Этапа 16 — дневной токен-бюджет Groq (100k TPD) реально исчерпался на живом трафике и держал бота на rule-based-фолбэке около 2 часов подряд в тот же вечер. Пользователь предложил Mistral, проверил и прислал организационные лимиты из `admin.mistral.ai` напрямую.
  - Перед кодом — та же дисциплина проверки фактов, что и на Этапах 15-16: прямой вызов `api.mistral.ai` с library-vps (без прокси) дал `401`, не `403` — в отличие от Groq/Gemini, Mistral (французская компания) Россию, похоже, не блокирует, туннель не обязателен. SDK (`mistralai==2.9.3`) — тоже сгенерирован Speakeasy, тот же приём `client=httpx.Client(proxy=...)`, что уже проверен на Groq. Схема (`llm/schema.py::PARAMETERS_SCHEMA`) используется без конвертации — tool calling Mistral совместим по форме с Groq/OpenAI, аналог `_to_gemini_schema` не понадобился. Единственная новая деталь: SDK типизирует `tool_calls[0].function.arguments` как `Union[dict, str]` (у Groq — всегда строка) — `llm/mistral_client.py` обрабатывает оба случая.
  - Лимиты Mistral — по токенам/запросам **в минуту**, не по дням, как у Groq — структурно не воспроизводят именно тот отказ, что случился на Этапе 16. Модель `ministral-8b-2512` выбрана явно датированной, не алиасом `-latest` — на странице лимитов `mistral-medium-latest` (25k TPM) оказался заметно хуже датированных `mistral-medium-2505`/`2508` (356-375k) — ровно та же ловушка, что уже стоила времени на Gemini (`gemini-flash-latest` резолвился в модель с 5 RPM), поймана заранее по факту наблюдения на странице лимитов, не на живом сбое.
  - `config.py` (`MISTRAL_API_KEY`/`MISTRAL_MODEL`, `LLM_PROVIDER` теперь принимает `mistral`), `llm/client.py` (диспетчер на три ветки), `requirements.txt` (`mistralai==2.9.3`). Новых юнит-тестов не добавлено — `_parse_arguments` (общий для всех провайдеров) не менялся, а `raw_analyze()` в проекте принципиально не мокается юнит-тестами ни у одного провайдера (тот же выбор, что уже сделан для Groq/Gemini) — вместо этого прямой живой вызов. 98/98 тестов зелёные без изменений.
  - **Живая проверка** — пока один ручной вызов, не `compare_llm_vs_rules.py`: «РН на Лыжной есть 95? Очередь большая?» → верно `question`/`rosneft_lyzhnaya`/`question_grades=["95"]` с первой попытки. Достаточно, чтобы подтвердить рабочую интеграцию, недостаточно для содержательного сравнения с rule-based на объёме, как у остальных двух провайдеров — открытый пункт на будущее.
  - Документация обновлена (CLAUDE.md, README.md, ARCHITECTURE.md — новый подраздел «Третий провайдер: Mistral», DEPLOY.md — таблица переменных и отдельная секция про то, что туннель Mistral не требуется).
  - **Не сделано в этом заходе**: деплой на `library-vps` — нужно решить, переключать ли там `LLM_PROVIDER` на `mistral` (или оставить `groq` по умолчанию, а `mistral` держать про запас) — отдельный шаг, ждёт решения пользователя.
- [x] **Этап 18 — Mistral на library-vps, дефолт-провайдер переключён на mistral везде** (2026-08-13): по прямому решению пользователя. `git pull` на сервере (одна накладка — `gazetteer.yaml` конфликтовал с ранее закинутой напрямую через `scp` записью про Китэк, содержимое идентично уже запушенному коммиту, `git checkout -- gazetteer.yaml` перед `pull` решил без потерь). `.env` на сервере дополнен `MISTRAL_API_KEY`/`MISTRAL_MODEL`, `LLM_PROVIDER` — `groq` → `mistral`. `config.py` (дефолт `LLM_PROVIDER` тоже сменён на `mistral` — не только на этом VPS, а везде, включая локальную разработку) и упоминания «groq по умолчанию» в CLAUDE.md/README.md/ARCHITECTURE.md/DEPLOY.md поправлены отдельным коммитом до деплоя. `docker builder prune -f` + `up -d --build --force-recreate app` — пересборка чистая, контейнер стартовал, VK-подключение в порядке, остальные 6 чужих контейнеров не задеты. Диск 1.4G→1.2G свободных (88% занято — новые зависимости `mistralai`), память без изменений.
  - **Живая проверка на сервере нашла реальную, воспроизводимую находку**: прямой вызов `analyze()` внутри контейнера на «Газпром бенз есть? Очередь?» трижды подряд (3/3, не разовая случайность) дал `question_grades=['ДТ']` — притом что «бенз» в переписке этого чата везде значит именно бензин, в противовес ДТ (см. например живую находку Этапа 5, «бензина не было, только дизель»). `station_id=gazprom` и `message_type=question` при этом резолвились верно оба раза. Менее серьёзно, чем находки на Groq (там LLM фабриковал факты прямо в `fuel_report` — порча БД; здесь это вопрос, ничего в БД не пишется, максимум — бот ответит не той маркой, которую спрашивали) — но реальный, не гипотетический риск качества у нового дефолтного провайдера, ещё не объяснённый (промпт исторически заточен под Groq, под Mistral вообще не проверялся и не дорабатывался). Не чинили точечно в этом заходе — тот же принцип, что и с находками на Groq/Gemini: зафиксировать с конкретным воспроизводимым примером, не гнаться за одним симптомом сразу. Кандидат на первоочередную проверку, когда до этого дойдут руки — прогнать `compare_llm_vs_rules.py` под `LLM_PROVIDER=mistral` на всех фикстурах, не на одном сообщении.
- [x] **Этап 19 — включён `AUTO_REPLY_ON_QUESTION` на живом деплое** (2026-08-14): по прямому запросу пользователя, после разбора живых логов. Пользователь заподозрил «проблему с ботом в VK» — проверка логов показала, что бот исправен (единственное реальное упоминание за ~9 часов трассировано от классификации до `messages.send`, ушло верно), но `AUTO_REPLY_ON_QUESTION=false` (унаследовано как есть из локального `.env` при переносе на Этапе 16) означает, что из 21 сообщения, распознанного как `question` за то же окно, ответ ушёл только на то одно — остальные были вопросами друг другу в чате, не боту, и корректно не отвечались. Пользователь отдельно указал на конкретную причину, почему это особенно важно исправить именно сейчас: с телефона через приложение VK бота, по его наблюдению, тегнуть невозможно (в отличие от ПК-браузера) — то есть при выключенном автоответе мобильная аудитория чата структурно не может получить ответ от бота вообще никаким способом. Причина самой разницы ПК/моб. приложение не выяснена (похоже на тот же класс явления, что и в Этапе 5 — «бот не показывается в списке участников/не тегается» во второй беседе, тогда тоже списано на платформенное поведение VK, не баг кода — не расследовано до конца ни тогда, ни сейчас).
  - `.env` на сервере: `AUTO_REPLY_ON_QUESTION=false` → `true`. `docker compose up -d --force-recreate app` — старт чистый, VK/Long Poll в порядке, остальные 6 чужих контейнеров не задеты. Значение по умолчанию в коде (`config.py`) и в `.env.example` НЕ менялось — это решение про конкретный живой чат (накопленная уверенность в точности на реальном трафике этого сообщества), не универсальный дефолт для гипотетических будущих установок бота с нуля.
  - Заодно, по тому же запросу («проверь использование API»): проверено вживую использование VK и Mistral API за последнее окно (с 21:44) — 75/75 вызовов Mistral `200 OK` (~8/час, далеко не под лимитом 625k TPM), VK — ни одной ошибки ни в одном из 4 использованных методов (`groups.getLongPollServer`/`getById`, `users.get`, `messages.send`). Оба API использовались чисто, без намёков на throttling с чьей-либо стороны.
  - **Первые ~55 минут вживую**: 31 сообщение обработано, 3 реальных ответа ушли в чат (было бы 1 при старом флаге) — автоответ подтверждённо работает, не просто настройка без эффекта. Ответы корректно оформлены (тег автора, оговорки о свежести). 0 ошибок, 0 рестартов, остальные контейнеры не задеты.
- [x] **Этап 20 — `AUTO_REPLY_ON_QUESTION` снова выключен** (2026-08-14): по прямому запросу пользователя, без указанной причины — обратная правка `.env` на сервере (`true` → `false`), `--force-recreate`, старт чистый. Код/дефолты не трогались (они и не менялись на Этапе 19). Живых проблем с автоответом до отключения замечено не было (см. выше) — решение пользователя, не реакция на конкретную находку.
- [x] **Этап 21 — простой бота на library-vps (~22 часа): бан от VK + ручная остановка пользователем, не сбой инфраструктуры** (2026-08-15): по запросу пользователя «включи бота на впс» — перед стартом `docker compose ps -a` показал оба контейнера `Exited` (`app` — код `137`/SIGKILL, `ai-proxy` — чисто `0`). Разбор логов `app`: контейнер стартовал 07:51:52 UTC (это хвост деплоя Этапа 20 — `--force-recreate` после правки `AUTO_REPLY_ON_QUESTION`), три часа работал нормально, с **10:52:07 UTC** каждый вызов `groups.getLongPollServer` стал возвращать `VKAPIError_27: Group authorization failed: group is banned` — само сообщество «Свидетель Бензовоза» (`club240747054`) было забанено VK. `vkbottle` (`BasePolling.handle_failed_event`) не делает бэкофф на такую ошибку — ретрай пошёл почти раз в секунду непрерывно (**10408** повторов в логе за ~2ч56м).
  - **Первая версия этой записи ошибочно предположила краш/OOM** — `docker inspect` показывал `OOMKilled=false` без ясного источника kill, а `ai-proxy` встал в ту же секунду, что выглядело как совпадение. **Пользователь поправил: остановка была намеренной** — он увидел бан сообщества в отдельном чате и попросил там остановить бота, это разумное решение (нет смысла долбить API забаненной группы). Проверка `journalctl -u docker` подтвердила: в 16:48:29 MSK (=13:48:29 UTC) обоим контейнерам ушёл `SIGTERM` через `docker compose stop`; `ai-proxy` уложился в штатный 10-секундный грейс-период и вышел чисто (`0`), `app` — нет, поэтому получил принудительный `SIGKILL` («Container failed to exit within 10s of signal 15 - using the force») — отсюда код `137`, это механика самой остановки (asyncio-луп `app` не реагирует на `SIGTERM` быстро), не признак краша. `restart: unless-stopped` **уже настроен на оба сервиса** в `docker-compose.yml` (в первой версии этой записи ошибочно сказано, что не настроен) — политика корректно НЕ подняла контейнеры обратно именно потому, что остановка была явной, это её штатное поведение, а не пробел. ~22 часа простоя объясняются просто тем, что после намеренной остановки никто не запускал бота обратно вручную до этого захода — не отсутствием автовосстановления.
  - Перед перезапуском — разовый диагностический вызов (`groups.getById`+`groups.getLongPollServer` напрямую через `docker compose run --rm --no-deps app python -c ...`, без поднятия всего бота): на 2026-08-15 бан снят, оба метода отвечают `200`. `.env` (`AUTO_REPLY_ON_QUESTION=false`, `LLM_PROVIDER=mistral`) не менялся с Этапа 20 — сверено перед стартом.
  - `docker compose up -d` (без пересборки — код не менялся): старт чистый, `groups.getById`/`getLongPollServer` прошли, Long Poll слушает. Остальные 6 чужих контейнеров на хосте (`travel-taxiptz`, `home-library-catalog`) не задеты, весь запуск отслежен `docker ps -a`/`df -h /`/`free -h` до и после — диск без изменений (1.1G свободно, пересборки не было), память в пределах обычного разброса (~365Mi available после старта против ~444Mi до).
  - **Реальный, всё ещё не починенный риск — именно ретрай-луп без бэкоффа, не отсутствие restart-политики**: почти раз в секунду 3 часа подряд бот долбил `groups.getLongPollServer` впустую, и узнали об этом только потому, что пользователь сам вручную заметил бан в VK и остановил бота в отдельном чате — автоматического обнаружения/алерта на застрявший в persistent-ошибке цикл нет. Не чинили в этом заходе — вне объёма исходного запроса «включи бота», кандидаты на будущее обсуждаются с пользователем отдельно.
- [x] **Этап 22 — статичные лимиты отпуска топлива по бренду в gazetteer** (2026-08-15): по прямому запросу пользователя — «сколько дают залить», «сколько можно», «сколько отпускают на рн/тн/гп» должны получать ответ, даже когда конкретная станция не резолвится. Пользователь дал значения: Роснефть 30 л, Татнефть 50 л, Лукойл 40 л.
  - **Осознанно НЕ через существующий `fuel_limit`/`StationLimit`** (Этап 14) — та таблица про факты из чата по конкретной станции. Это другое: константа на весь бренд, известная заранее, а не сообщённая кем-то в переписке. Новая секция `gazetteer.yaml::brand_fuel_limits` (бренд → литры), только для трёх названных брендов — остальные (Газпром, Опти, Китэк) остаются без записи, вопрос по ним без резолва станции по-прежнему без ответа, как и раньше.
  - **Почему это вообще отдельная задача, не просто данные**: у Роснефти/Татнефти/Лукойла по нескольку точек, и `resolve_station()` намеренно не резолвит голый бренд без адреса ни в одну из них (кроме брендов с `default_for_brand`, сейчас только Газпром) — это подтверждено существующим тестом (`test_pure_limit_report_resolves_station`, «Роснефти» без адреса → `None`). Значит, «сколько на РН» в принципе не может дойти до ответа через обычный путь `answer_question(station_id=...)` — там `station_id=None` при входе всегда означает молчание. Добавлены `pipeline/resolve_station.py::resolve_brand()` (бренд, если в тексте однозначно назван ровно один, независимо от резолва конкретной станции) и `get_brand_fuel_limit()`, плюс `pipeline/qa.py::answer_brand_limit_question()` (свой regex «похоже на вопрос про лимит» + резолв бренда + лимит из газетира) — вызывается из `pipeline.py` только когда обычный `answer_question` вернул `None` именно из-за `station_id is None`, не подменяет ответ по резолвленной станции.
  - **Сознательно НЕ стали** делать статичный лимит фоллбэком и для уже резолвленной станции без единого отчёта в чате (например «сколько на Лыжной льют?» при пустой `fuel_limit` для этой станции) — это сломало бы существующий тест (`test_answer_question_no_data_for_station`) и прямо противоречило бы задокументированному решению Этапа 5 «станция известна, но данных нет → бот молчит», которое не было предметом этого запроса.
  - **Regex на «похоже на вопрос про лимит» откалиброван по двум реальным сообщениям из чата** (`tests/fixtures.py`, `QUESTIONS`), не только по трём формулировкам из запроса пользователя: «Подскажите сколько лимит на Роснефти» и живая находка Этапа 14 «Татнефть сколько заливают?» — оба покрыты явно, тестами. Не проверялось на живом трафике беседы (бот на VPS не перезапускался и не передеплоивался в рамках этого этапа) — только юнит-тестами и прямым локальным вызовом на синтетических и реальных-из-фикстур формулировках.
  - LLM-путь (`llm/schema.py`/`llm/prompts.py`) не тронут вообще — фоллбэк чисто rule-based и подключён в `pipeline.py` уже после того, как оба пути (LLM и rule-based) отработали и не дали `station_id`, так что работает одинаково независимо от `LLM_ENABLED`.
  - 107/107 тестов зелёных (9 новых: `resolve_brand`/`get_brand_fuel_limit` в `test_pipeline.py`, `answer_brand_limit_question` в `test_qa.py`, два сквозных через `process_message` — на реальной формулировке из чата и на проверке, что фоллбэк не подменяет ответ по резолвленной станции).
  - Не задеплоено на library-vps в рамках этого этапа — только локальные изменения в репозитории.
- [x] **Этап 23 — новая станция в gazetteer: СТК (Вытегорское шоссе)** (2026-08-15): по прямому запросу пользователя — "на вытегорском (вытегорка), напротив автоспецтранса (аст), компания СТК". Новый бренд, единственная точка — `stk_vytegorskoe`, резолвится по одному `brand_alias` ("стк"), без уточнения локации (см. правило в начале gazetteer.yaml); `location_aliases` ("вытегор", "автоспецтранс", "аст") добавлены на случай упоминания без бренда — все три проверены как уникальные в газетире, коллизий с другими станциями нет. Заодно — реальный пробел, не специфичный для этой станции: `pipeline/prefilter.py::_KEYWORD_PATTERN` (on-topic фильтр, первый гейт перед резолвом) перечисляет бренды по имени, "стк" туда тоже добавлено — без этого чистое сообщение про станцию без цифр/"?"/других ключевых слов отсеивалось бы до резолва. Такой же пробел существует и для "китэк"/"китек" (не добавлены туда при Этапе 15) — не трогали, вне объёма этого запроса, только зафиксировано здесь на будущее.
  - 107/107 тестов зелёных без изменений (это чистое расширение данных в gazetteer.yaml — `resolve_station()`/`is_on_topic()` уже были покрыты тестами на существующих станциях; для отдельных станций в проекте по прецеденту (Китэк, Этап 15) выделенных юнит-тестов не заводят). Прямой локальный вызов `resolve_station()`/`is_on_topic()` подтвердил резолв по бренду, по локации без бренда, по ориентиру "аст" без бренда, и что бренд с чужим location_alias всё равно резолвится в свою (единственную) точку, как и положено при `_BRAND_COUNTS == 1`.
  - Не задеплоено на library-vps — только локальные изменения в репозитории.
- [x] **Этап 24 — деплой Этапов 22-23 на library-vps** (2026-08-15): по запросу пользователя «сделай деплой». Перед `git pull` — проверка `git status` на сервере (чистое дерево, без расхождений с прошлым Этапом 18) и внимание к тому, что `gazetteer.yaml` там правится и вручную (см. CLAUDE.md, DEPLOY.md) — конфликта не случилось. Сервер стоял на `634bedc` («Make mistral the default LLM_PROVIDER») — это ДО коммита с пометкой Этапа 18 в PROGRESS.md; `git pull` не понадобился ни на Этапе 19, ни на 20, ни на 21, потому что там менялся только `.env`, а не код — с Этапа 18 это первый деплой с реальными изменениями в `.py`.
  - **Python-код не примонтирован** (bind-mount в `docker-compose.yml` — только `bot.db`(+`-shm`/`-wal`) и `gazetteer.yaml`), в отличие от чистых правок gazetteer.yaml он запекается в образ на сборке — поэтому даже без новых зависимостей нужен `docker compose up -d --build`, а не просто рестарт. `docker builder prune -f` перед сборкой (1.1G свободно на диске) освободил 180MB; сборка почти вся из кэша (менялся только слой `COPY . .`), быстро.
  - Старт чистый: `docker compose ps` — оба `Up`, `groups.getById`/`getLongPollServer` прошли, Long Poll слушает. Остальные 6 чужих контейнеров на хосте не задеты. `df -h /`: 1.3G свободно (без изменений от сборки). `free -h`: ~374Mi available — в пределах обычного разброса.
  - **Живая проверка внутри уже задеплоенного контейнера** (`docker compose exec app python -c ...`, не только доверие к тому, что `git pull`+`--build` отработали) — обе новые фичи подтверждены на реальном сервере: `answer_brand_limit_question` верно отвечает по всем трём брендам, `resolve_station` резолвит новую станцию `stk_vytegorskoe` и по бренду, и по голой локации «вытегорке».
- [x] **Этап 25 — разбор живых логов после деплоя + два быстрых фикса** (2026-08-15): по запросу пользователя «посмотри как там дела». За первые ~5 часов на новом коде: 243 сообщения, 0 ошибок/трейсбэков, 0 рестартов, диск/память в норме. 19 вопросов, `AUTO_REPLY_ON_QUESTION=false` (см. Этап 20) — ответ уходит в чат, только если бота явно тегнули; большинство вопросов, как и раньше, остаются без ответа в чате (не баг, старое поведение).
  - **Обе фичи Этапов 22-23 уже отработали на органическом трафике**: реальное «Стк на тидена бензин завезли» верно резолвнулось в `stk_vytegorskoe` (сработало только потому, что СТК — единственная точка бренда; без слова «стк» «тидена» одна не резолвила бы). Реальное «Ра РН какой лимит сейчас?» (без тега бота) внутри пайплайна корректно дошло до `answer_brand_limit_question` и посчитало «Роснефть: лимит 30 л» — но `vk_handlers.py` не отправил ответ в чат именно из-за `AUTO_REPLY_ON_QUESTION=false`, не из-за бага новой фичи (`vk_handlers.py:100-107` — `reply_text` считается до проверки `is_mentioned`/`AUTO_REPLY_ON_QUESTION`, отбрасывается после).
  - **По итогам разбора, два фикса по прямому запросу пользователя**: (1) `gazetteer.yaml` — «тиден» добавлен в `location_aliases` станции `stk_vytegorskoe` (реальное разговорное название, подтверждённое тем самым сообщением). (2) `pipeline/extract.py::_UNLIMITED_RE` — «полный бак»/«до полного» рядом (≤15 символов, в любом порядке) со словом-разрешением (дают/наливают/заливают/можно/разрешают) теперь тоже даёт `status="unlimited"`, как и «без ограничений» — тоже реальная находка из того же сообщения («На стк полный бак дают + в канистру» раньше уходило в `irrelevant`). Требование слова-разрешения рядом — осознанно, чтобы личный рассказ «залил полный бак, спасибо» не читался как факт про политику станции; закрыто отдельным тестом-негативом.
  - 110/110 тестов зелёных (3 новых). Живой локальный вызов подтвердил: реальное сообщение про СТК теперь `report`/`unlimited`; «тидена» без «стк» резолвит станцию (хотя всё сообщение целиком по-прежнему уходит в `irrelevant` — известный, задокументированный с Этапа 2 пробел «наличие без явной марки», не предмет этого фикса); негативный пример по-прежнему `irrelevant`/`limit_info=None`.
  - Не задеплоено на library-vps в рамках этого этапа.
- [x] **Этап 26 — команды `!вкл`/`!выкл` в чате для управления `AUTO_REPLY_ON_QUESTION`** (2026-08-15): по прямому запросу пользователя, после обсуждения — «админ только я», команда голая `!вкл`/`!выкл`, без тега бота.
  - **Состояние — в БД, не в `.env`.** Решение принято на этапе обсуждения (см. предыдущий заход): бот не должен сам переписывать свой конфиг-файл на лету — хрупко и не переживает `--force-recreate`, который перечитывает `env_file` заново. Новая таблица `auto_reply_setting` (`db/schema.py`) — append-only, той же формы, что `station_break`/`fuel_limit` (Этапы 11/14): не `UPDATE`, а новая строка на каждое переключение, текущее значение — последняя по `id` (не по `changed_at` — секундного/микросекундного совпадения таймстампов при быстрых подряд вызовах достаточно, чтобы `ORDER BY changed_at` стал недетерминированным; `id AUTOINCREMENT` гарантирует порядок вставки всегда). `db/repo.py::get_auto_reply_enabled(conn, default=...)`/`set_auto_reply_enabled(...)`. `.env`-значение `AUTO_REPLY_ON_QUESTION` осталось дефолтом на случай, если командой ни разу не воспользовались (свежий `bot.db` или БД, где таблица ещё пуста).
  - **Разбор команды — до обычного пайплайна**, `vk_handlers.py::_parse_admin_command()` (голый текст целиком после `strip().lower()`, без тега бота и без хвостов — "!вкл, пожалуйста" не считается) — если распознано И `message.from_id == ADMIN_ID`, команда обрабатывается и функция возвращается сразу, не доходя до `is_on_topic`/`process_message`/LLM. Не-админ, написавший "!вкл" — просто уходит в обычный пайплайн и там же в `off_topic` (ни цифр, ни "?", ни ключевых слов темы), без явного отказа в духе "вам нельзя" — тише, меньше шума в чате.
  - `ADMIN_ID` (был в `.env`/`config.py` с самого начала проекта, но нигде не использовался — см. DEPLOY.md, таблица переменных, поправлено) теперь реально проверяется. Подтверждение переключения — прямым сообщением в чат ("Автоответ на вопросы включён/выключен."), без тега (это ответ администратору на его же команду, не факт из БД, поэтому в обход `templates.py`/тега автора).
  - Триггер вопроса (`vk_handlers.py`, было `AUTO_REPLY_ON_QUESTION` напрямую) теперь читает `repo.get_auto_reply_enabled(_conn, default=AUTO_REPLY_ON_QUESTION)` — живое значение из БД на каждом сообщении, не значение, зафиксированное на момент импорта модуля.
  - 116/116 тестов зелёных (6 новых: `tests/test_repo.py` — новый файл, get/set/дефолт/последняя-запись-побеждает; `tests/test_vk_handlers.py` — `_parse_admin_command` на команды/регистр-и-пробелы/обычный текст). `on_message` целиком (как и раньше — см. существующие тесты `_quoted_context_text`/`_recent_author_message`) не юнит-тестируется — слишком завязан на объекты vkbottle, только вынесенные чистые хелперы. Проверено локально: миграция схемы чисто применяется к реальному `bot.db` (`CREATE TABLE IF NOT EXISTS`, без вставки данных, по образцу Этапа 11), полный ручной round-trip (default → `!вкл` → `!выкл`) на `:memory:` дал ожидаемые значения на каждом шаге.
  - Не задеплоено на library-vps и не проверено живым сообщением в чате в рамках этого этапа — `on_message`-обвязка (тег ADMIN_ID, `message.reply`) принципиально требует живого VK, не проверяется локально.
- [x] **Этап 27 — деплой Этапов 25-26 на library-vps + реальный `ADMIN_ID`** (2026-08-15): по запросу пользователя «да, деплой». Сервер стоял на Этапе 23 (Этап 24 задеплоил только его) — `git pull` подтянул сразу оба, чистый fast-forward. Код менялся (`vk_handlers.py`, `db/*.py`, `pipeline/extract.py`) — нужна пересборка (`docker builder prune -f` + `up -d --build`), не просто рестарт; старт чистый, остальные 6 чужих контейнеров не задеты, диск/память в норме.
  - **Живая находка сразу после деплоя**: `ADMIN_ID` — пустой и на сервере, и (неожиданно) локально тоже, хотя ранее в этом же заходе локальная проверка (`grep -c`) ошибочно посчитала его «заданным» — грепалась сама строка `ADMIN_ID=`, не то, что после `=`. Значит команды `!вкл`/`!выкл` были бы недоступны вообще никому, включая пользователя, если бы не заметили до реального теста. Пользователь дал свой числовой VK id (`3022748`) — не угадывался и не брался из логов трафика без прямого подтверждения, специально чтобы не назначить админом не того по ошибке. Прописан и локально (`.env`, не коммитится — в `.gitignore`), и на сервере (точечный `sed` по строке `^ADMIN_ID=`, остальные секреты в файле не затронуты) — `docker compose up -d --force-recreate app` (только `.env` поменялся, пересборка не нужна). Внутри контейнера подтверждено: `config.ADMIN_ID == 3022748`.
  - **Живая проверка, тем же вечером**: пользователь отправил `!вкл` в беседе (`peer_id=2000000002`, `from_id=3022748`) в 18:10:10. Лог подтверждает полный путь: команда распознана, `messages.send` с текстом «Автоответ на вопросы включён.» ушёл и принят VK (`conversation_message_id=45914`). `auto_reply_setting` — ровно одна строка, `enabled=1, changed_by=3022748, changed_at=2026-08-15T18:10:10`, точно на то же время.
  - **Эффект подтверждён отдельно, в тот же вечер (18:13:46)**: реальный органический вопрос от НЕ-админа («Тот же вопрос)», реплай на чужое «Как дела на РН у куковского кольца?», от Марии, `from_id=800496696`, **без тега бота**) получил содержательный ответ («…информация по Роснефть (Лесной просп. 40/38, у РБ)… отстой топлива… 92, 95 - Есть»). До `!вкл` такой неотмеченный вопрос остался бы без ответа (см. Этап 19/20) — это прямое, живое доказательство, что переключатель не просто пишется в БД, а реально меняет поведение. 0 ошибок в логе за всё время с деплоя.
- [x] **Этап 28 — живая находка: LLM (Mistral) уверенно резолвнул несуществующий алиас, фикс газетиром** (2026-08-15): прямое следствие Этапа 27 — как только `!вкл` сделал ответы на неотмеченные вопросы видимыми всем, пользователь заметил, что ответ выше (про «куковского кольца») был неверным: `rosneft_lesnoy_40` и станция у «куковского кольца» — разные АЗС. Разбор лога подтвердил `path=llm message_type=question station_id=rosneft_lesnoy_40` — то есть это не rule-based (там для этого текста корректно вышел бы `None`, ни один location_alias не матчился), а Mistral без всяких оснований в тексте выбрал станцию среди пяти точек Роснефти. Тот же класс риска, что уже задокументирован (Этапы 5, 16, 18) — не новый баг, но раньше был не виден почти никому (ответ уходил только по тегу бота), а теперь, при включённом автоответе, любая такая ошибка резолва уходит в чат всем сразу — стоит держать в уме, пока `!вкл` активен.
  - **По прямому уточнению пользователя** — «куковка»/«куковское кольцо» оказалось разговорным названием уже существующей станции `rosneft_komsomolsky` («Роснефть, Комсомольский просп. / у Ленты»), не новой АЗС. `location_aliases` дополнены тремя вариантами: «куковк» (Куковка), «куковск» (куковское/куковского — как в реальном сообщении в чате), «кукковск» (кукковское — как написал пользователь, двойное к, на случай обоих написаний у разных людей).
  - **Живая проверка именно там, где была ошибка** — не только локальный `resolve_station()` (rule-based, для этого текста и раньше не был виноват), а прямой повторный вызов `llm.mistral_client.raw_analyze()` с тем же самым текстом (own_text="Тот же вопрос )", quoted_context="Как дела на РН у куковского кольца?") против реального Mistral API: **3 из 3** повторов теперь дают `station_id=rosneft_komsomolsky` (было — уверенно и неверно `rosneft_lesnoy_40`). `llm/prompts.py::_stations_brief()` читает `gazetteer.yaml` заново при каждой сборке промпта (не кэширует на импорте, в отличие от `resolve_station.py::_STATIONS`) — новый алиас подхватился без изменений в коде LLM-пути.
  - 116/116 тестов зелёных без изменений (чистое расширение данных в gazetteer.yaml, `resolve_station()` уже покрыт тестами на существующих станциях).
  - Не задеплоено на library-vps в рамках этого этапа — только локальные изменения в репозитории.
- [x] **Этап 29 — команда `!лимит`/`!лимиты` (для всех) + лимиты Газпрома/СТК** (2026-08-15): по прямому запросу пользователя. Два независимых, но переплетённых изменения.
  - **`gazetteer.yaml::brand_fuel_limits` расширен со статуса «только число литров» до `{status: limited, liters: N}` / `{status: unlimited}`** — та же форма, что уже была у `LimitInfo`/`StationLimit` (`pipeline/extract.py`/`pipeline/facts.py`), переиспользована напрямую (`get_brand_fuel_limit()` теперь возвращает `LimitInfo`, не голое число) вместо изобретения нового класса. Добавлены записи: Газпром — `unlimited`, СТК — `unlimited` (оба по прямому сообщению пользователя). `pipeline/qa.py::answer_brand_limit_question()` — новая ветка на `status == "unlimited"` («Газпром: без ограничений.»), формат для `limited` не менялся.
  - **Новая команда `!лимит`/`!лимиты`** (оба написания, единственное и множественное) — в отличие от `!вкл`/`!выкл`, доступна всем, без проверки `ADMIN_ID`. Показывает не весь `brand_fuel_limits`, а курируемое подмножество — новое поле `gazetteer.yaml::brand_fuel_limits_display: [Роснефть, Татнефть, Лукойл, Газпром]`, по прямому решению пользователя (СТК сознательно не включён, хотя у него теперь тоже есть запись в `brand_fuel_limits` — единственная точка бренда, погоды не делает; Опти/Китэк лимит вообще не известен). `pipeline/resolve_station.py::get_displayed_brand_fuel_limits()` (бренды и лимиты в заданном порядке) → `templates.py::render_brand_limits_list()` (рендер, отдельная функция, не переиспользует `_render_limit_line` — разный формат, там факт из чата с «сообщили N назад», здесь статичный список без возраста). `vk_handlers.py::_is_limit_list_command()` — разбор до обычного пайплайна, как у `!вкл`/`!выкл`, но без гейта по `ADMIN_ID`; дебаунс (`_can_reply`) сохранён — команда открыта всем, риск флуда реальнее, чем у админ-only `!вкл`/`!выкл` (там дебаунс сознательно не ставили).
  - 122/122 тестов зелёных (6 новых). Живой локальный вызов подтвердил точный формат вывода `!лимит` (4 бренда, СТК отсутствует) и оба филиала `answer_brand_limit_question` (limited/unlimited).
  - Не задеплоено на library-vps в рамках этого этапа.
- [x] **Этап 30 — актуализация и сокращение всех MD-файлов, кроме PROGRESS.md** (2026-08-15): по прямому запросу пользователя. `PROGRESS.md` сознательно не трогали — это журнал этапов (см. собственный заголовок файла), сокращать историю задним числом значит терять её как аудит-трейл; если пользователь имел в виду и его — отдельный запрос.
  - **`README.md`**: `ADMIN_ID` был описан неверно («для review нераспознанных станций» — это не имеет отношения к тому, как переменная реально используется, `scripts/review_unresolved.py` её вообще не читает) — поправлено на актуальное («доступ к `!вкл`/`!выкл`»). Добавлена секция «Команды в чате» — раньше не упоминались вообще.
  - **`ARCHITECTURE.md` — самая большая правка, 145→101 строка при том, что добавилось освещение шести новых с прошлой правки компонентов** (`fuel_limit`, `brand_fuel_limits`, `auto_reply_setting`, обе чат-команды, `!лимит`/`!лимиты`). Секция «LLM-анализ» была фактически конденсированным девлогом (точные проценты совпадения, датированные «живые тесты» по провайдерам) — уже задокументировано подробнее в PROGRESS.md (Этапы 4-6, 15-18), здесь сжато до текущего состояния+ссылки. Секция «Возможное развитие» — вычищены пункты, решённые давно и зачёркнутые (`~~...~~`), не несущие ничего сверх PROGRESS.md; переименована в «Известные пробелы и открытые вопросы», куда добавлены два реальных, всё ещё не закрытых пункта, раньше существовавших только в PROGRESS.md как разрозненные находки: ретрай-луп без бэкоффа на persistent-ошибках VK API (Этап 21) и то, что `!вкл` делает любую ошибку LLM-резолва видимой всему чату сразу (Этап 28). Найдена и исправлена фактическая ошибка: «на library-vps включён только `LLM_PROVIDER=groq`» — устарело с Этапа 18, дефолт `mistral`.
  - **`DEPLOY.md`**: вступление переформулировано — раньше фокусировалось на необходимости SOCKS5-туннеля для Groq/Gemini, не упоминая, что дефолтный провайдер (`mistral`) в нём вообще не нуждается (реальное текущее состояние деплоя). Шаг 6 процедуры давал пример `LLM_PROVIDER=groq` — заменено на актуальный `mistral`, с пояснением что шаг проверки туннеля для него не нужен. Список таблиц в «Бэкапы» дополнен `auto_reply_setting`.
  - **`CLAUDE.md`**: LLM-пункт освобождён от точных исторических цифр совпадения (со ссылкой на PROGRESS.md вместо них — то же решение, что и в ARCHITECTURE.md). Пункты «Оба режима триггера» и «`!вкл`/`!выкл`» объединены — были об одном и том же, дублировали контекст. Остальные пункты (гвард на `own_text`, `replace_mention`, пороги свежести, один `station_id` на сообщение и др.) не трогались — каждый уже был компактным и специфичным, дальше сокращать значило бы терять конкретику, которая и есть весь смысл файла.
  - 122/122 тестов зелёных без изменений (правка только `.md`, код не тронут).
  - Не задеплоено на library-vps — деплоя не требует, `.md`-файлы не входят в образ и не влияют на работу бота.
- [x] **Этап 31 — деплой Этапов 28-30 на library-vps** (2026-08-15): по запросу пользователя «делай деплой». Сервер стоял на Этапе 26 — `git pull` подтянул сразу три этапа (алиас «куковка», `!лимит`/`!лимиты` + лимиты Газпрома/СТК, обновлённые MD-файлы), чистый fast-forward. Код менялся (`pipeline/qa.py`, `pipeline/resolve_station.py`, `templates.py`, `vk_handlers.py`) — пересборка (`docker builder prune -f` + `up -d --build`). Старт чистый, остальные 6 чужих контейнеров не задеты, диск 1.3G свободно, память ~422Mi available.
  - **Живая проверка внутри контейнера**: `!лимит`/`!лимиты` выдаёт ровно 4 бренда в заданном формате; `answer_brand_limit_question` верно отвечает «без ограничений» и для Газпрома, и для СТК; резолв «куковского кольца» (находка Этапа 28) подтверждён и на задеплоенном коде.
- [x] **Этап 32 — очередь как структура вместо свободного текста модели (закрытие F1)** (2026-08-16): первый этап реализации по [ARCH_DECISIONS.md](ARCH_DECISIONS.md), решение Р2. Предыстория — технический аудит (AUDIT_FINDINGS.md, F1): `queue_note` было единственным полем, куда модель писала свободный текст, валидация проверяла только «непустая строка», и оттуда текст уходил в чат **от имени сообщества**, с живой VK-разметкой упоминаний и ссылками, плюс оседал в append-only логе и повторялся в каждом будущем ответе про станцию. Три инъекции из трёх на живом Mistral сработали.
  - **Форма.** Новый DTO `QueueInfo` (`pipeline/facts.py`, `frozen=True` — нужен хешируемым, `templates.py` группирует марки по ключу с очередью внутри): `status` («none»/«present») + `minutes`/`cars_from`/`cars_to`. Живёт в `facts.py`, а не рядом с `LimitInfo`/`BreakInfo` в `extract.py`, сознательно: одна и та же форма нужна и на записи, и на чтении, а `extract.py` уже импортирует `facts.py` — обратный импорт дал бы цикл.
  - **Схема LLM** (`llm/schema.py`): поле `queue_note: string` заменено на объект `queue` с enum-статусом и целочисленными полями; `_parse_queue_info()` отбрасывает строку вместо объекта, неизвестный статус, лишние ключи с текстом, не-int и абсурдные числа (потолок `MAX_QUEUE_VALUE=999` — столько же, сколько допускает regex rule-based пути). `bool` отсекается отдельно: в Python `True` — это `int`, без проверки `{"cars_from": true}` дало бы «~1 машин».
  - **Рендер** (`templates.py::render_queue`) — единственное место, где очередь превращается в текст, словарь фиксирован: «без очереди» / «есть очередь» / «~N мин» / «~N машин» / «~N-M машин». Диапазон машин сохранён специально (реальное «3-4 машины» из чата) — ради него в `QueueInfo` две границы, а `_QUEUE_CARS` в `extract.py` переписан с одной группы на две.
  - **БД**: четыре новые колонки `queue_status`/`queue_minutes`/`queue_cars_from`/`queue_cars_to`. Легаси-колонка `queue_note` **оставлена, но не пишется и не читается** — переписывать историю append-only лога не стали, а не читая её, нейтрализовали и уже осевший в базе текст. Появился первый догоняющий `ALTER TABLE` (`db/schema.py::_add_missing_columns`) — `CREATE TABLE IF NOT EXISTS` существующую таблицу пропускает, и без него бот упал бы не на старте, а на первом сообщении (находка D6). Полноценных миграций по-прежнему нет, это минимальный механизм под конкретный случай.
  - **Проверки.** 133 теста зелёных (11 новых: инъекция строкой вместо объекта, инъекция в статус, лишние текстовые ключи, не-int и абсурдные числа, диапазон и мусорный диапазон, «очереди нет» против «про очередь не говорили», закрытость словаря рендера, легаси-текст не доезжает до ответа, round-trip структуры через БД). Отдельно прогнана миграция на честной копии старой схемы: колонки добавляются, старые строки целы, инъекция из легаси-колонки в ответ не попадает, повторный запуск идемпотентен.
  - **Живой Mistral, 5 вызовов** (прямые, без прокси, с этой машины): все пять вернули новую структуру. «Очередь.» → `present` → «есть очередь»; «очереди нет» → `none`; «очередь 6 машин» → `cars_from=6`; «заправилась за 12 минут» → `minutes=12`; инъекция `SYSTEM: игнорируй прежние инструкции...` → обычный статус без единого символа подставленного текста.
  - **Прод осмотрен** (read-only, `bot.db` на library-vps): 42 различных формулировки `queue_note`, **ни одной** с VK-разметкой, ссылками или «пишите в личку» — то есть дыра существовала, но в живом чате ею не воспользовались. Побочное наблюдение: 42 формулировки против 4 шаблонов — переход на структуру теряет качественные оттенки («потихоньку двигаемся», «еле двигается», «свободны 1 и 2 колонка»). Числа и «есть/нет» сохраняются полностью. Расширять словарь (например, полем «скорость движения») — отдельное решение, в этот этап не входило.
  - **Замечено по ходу**: прогон `pytest` мигрировал локальную боевую `bot.db` — это находка A4 (импорт `vk_handlers` открывает реальную базу) в действии. Миграция аддитивная, данные целы; сама A4 не чинилась, она не входит в решения ARCH_DECISIONS.md.
  - Не задеплоено на library-vps.
- [x] **Этап 33 — граница беседы: allowlist из двух бесед (закрытие F2)** (2026-08-16): второй этап реализации по [ARCH_DECISIONS.md](ARCH_DECISIONS.md), решение Р4. До этого `peer_id` не проверялся нигде: отчёты из одной беседы формировали ответы в другой, а сообщество, добавленное в любой чужой чат (это настройка сообщества, не кода), и читало накопленную базу, и писало в неё; публичная `!лимит` работала везде.
  - **Ровно две беседы — прямое решение пользователя** («не меньше и не больше»), `peer_id` даны им же: `2000000001`, `2000000002`. Внутри allowlist **база фактов общая** — тоже прямое решение: обе беседы про одни и те же АЗС, общая история вдвое плотнее, а наружу уходит только марка/статус/время, без текста сообщения и без автора. Разговорный контекст (`_last_author_message`) как был разделён по беседам, так и остался.
  - **Гейт первым действием** в `vk_handlers.py::on_message` — до дедупа, до разбора команд, до всего: из чужой беседы не читаем, не пишем и даже не помечаем сообщение обработанным. Первое сообщение из незнакомой беседы логируется **один раз** (`_unknown_peers_logged`) — владелец узнаёт, что сообщество куда-то добавили, но чужой чат не может залить лог строкой на каждое сообщение.
  - **`ALLOWED_PEER_IDS` обязательна, дефолта нет намеренно**: пустое значение пришлось бы трактовать как «любая беседа» — ровно та дыра, которую закрываем. `config.py::_parse_allowed_peer_ids()` кидает `ValueError` с внятным текстом. Вынесено в функцию именно ради тестируемости: инлайн-код на уровне модуля проверить нечем. **Для деплоя важно**: переменную надо добавить в `.env` на сервере ДО рестарта, иначе контейнер уйдёт в цикл перезапусков (записано в DEPLOY.md и README.md).
  - **Первое в проекте покрытие тела `on_message`** (до сих пор 0%, находка J1): тесты гоняют настоящий зарегистрированный обработчик (`bot.labeler.views()["message"].handlers[0].handler`) на подставном `Message` и in-memory базе. Подмена `vk_handlers._conn` обязательна — модуль открывает боевую `bot.db` прямо на импорте (находка A4), без неё тест писал бы в неё.
  - 148 тестов зелёных (10 новых): чужая беседа не оставляет ни факта, ни отметки дедупа; контроль — то же самое сообщение из разрешённой беседы записывается; общая база подтверждена сквозняком (отчёт → `answer_question`); лог о чужой беседе ровно один на три сообщения; разбор переменной — пустая/из пробелов/из запятых/нечисловая/с лишними пробелами. Отдельно проверено живьём: из каталога без `.env` и без переменной `import config` падает с нужным сообщением.
  - Не задеплоено на library-vps.
- [x] **Этап 34 — деплой Этапов 32-33 на library-vps** (2026-08-16): по запросу пользователя «сначала деплой». Сервер стоял на `fff7f48` (Этап 30), дерево чистое — `git pull` чистым fast-forward подтянул сразу и документы аудита, и оба этапа реализации.
  - **Порядок был важен.** `ALLOWED_PEER_IDS` обязательная и без дефолта (Этап 33), поэтому её дописали в `.env` на сервере **до** пересборки — иначе контейнер ушёл бы в цикл перезапусков, а снаружи это выглядит как обычное молчание бота. Перед этим `.env` скопирован в `.env.bak-2026-08-16`.
  - **Бэкап БД перед миграцией схемы** (Этап 32 добавляет 4 колонки): онлайн-бэкап через `sqlite3.Connection.backup()`, а не `cp` — база под WAL и с работающим контейнером, обычное копирование не атомарно. `bot.db.bak-2026-08-16-pre-stage32`, в нём 310 отчётов, 19 перерывов, 7 лимитов, 30 нераспознанных, 2 записи переключателя. Лежит на сервере рядом с базой (бэкапов вне VPS по-прежнему нет — см. DEPLOY.md, «Бэкапы»).
  - Код менялся — `docker builder prune -f` + `docker compose up -d --build --force-recreate` (отсоединённо, `nohup`, по нюансу из DEPLOY.md). Старт чистый: `groups.getById`/`getLongPollServer` прошли, Long Poll слушает, **0 ошибок и трейсбэков** за 20 минут. Остальные 6 чужих контейнеров не задеты, диск 1.3G свободно (без изменений), память 360Mi available, `app` — 107 МБ.
  - **Миграция применилась на боевой базе**: колонки `queue_status`/`queue_minutes`/`queue_cars_from`/`queue_cars_to` на месте, 129 строк с легаси-текстом целы и больше не читаются. `ALLOWED_PEER_IDS` внутри контейнера — ровно `[2000000001, 2000000002]`.
  - **Живая проверка на органическом трафике, без единого подставленного сообщения.** Реальный отчёт «Репникова, Татнефть только что заправилась. Очередь небольшая, двигается шустро. Льют в канистры свободно, есть все» разобран через `path=llm` в три факта (92/95/ДТ, все available) со структурной очередью `status=present` — в чат это ушло бы как «есть очередь». Реальный вопрос «Ломоносова есть 95?» разобран через `path=llm`. То есть новый формат работает на живых, не подготовленных заранее формулировках.
  - **Цена решения Р2 видна на этом же примере**: «Очередь небольшая, двигается шустро» раньше сохранилась бы как есть и так же ушла бы в чат, теперь это «есть очередь». Оттенок потерян, факт — нет. Ровно то, о чём предупреждали при принятии решения; расширение словаря (например, полем «скорость движения») остаётся отдельным, не принятым решением.
- [x] **Этап 35 — надёжность обработчика и алерт владельцу (закрытие G1, E4, частично G3-G5)** (2026-08-16): третий этап реализации по [ARCH_DECISIONS.md](ARCH_DECISIONS.md), решения Р6 и Р8.
  - **Отступление от буквы Р6, по данным с прода.** В решении было записано «UNIQUE на факты, чтобы повторный разбор не дублировал». Проверка боевой базы показала, что такой индекс **невозможен**: четыре сообщения уже дали по 2-3 строки на одну марку — `CREATE UNIQUE INDEX` на существующих данных просто не создастся, и бот не стартует. Разбор этих сообщений показал и худшее: сообщение «Раз пошли дурацкие измышления… Во всём виноваты те, кто заправляют 95. Если на заправке нет 95, то и очереди нет» записалось пятью фактами, включая одновременно `95 available` и `95 unavailable` — то есть индекс произвольно оставлял бы первую строку из противоречивой пары. **Цель Р6 достигнута другим механизмом**: идемпотентность даёт не индекс, а одна транзакция на сообщение (см. ниже). Это ещё и закрывает D5, который в решениях числился неохваченным.
  - **Одно сообщение — одна транзакция.** Факты и отметка «обработано» кладутся вместе (`with conn:` в `process_message`); `db/repo.py` больше не коммитит сам — транзакцией владеет вызывающий (исключение — `set_auto_reply_enabled`, вызывается вне пайплайна). Вызов LLM сознательно оставлен **до** транзакции: держать открытую запись секундами ради сетевого вызова нельзя, тем более что дальше обработка станет конкурентной (Этап Р3). Отметка — внутри пайплайна, а не в `vk_handlers`, именно потому, что иначе её не положить в ту же транзакцию.
  - **Отметка «обработано» — после обработки, а не до** (G1). Пути с командами в чате (`!вкл`/`!выкл`/`!лимит`) до пайплайна не доходят и помечаются отдельно, `_mark_processed()`.
  - **E4 закрыт**: `users.get` (декоративный тег имени) обёрнут в `try/except` — отказ означает ответ без тега, а не уничтоженный ответ. Плюс один ретрай на отправку (`_reply_with_retry`).
  - **Перехват вокруг всей обработки**: тело вынесено из замыкания в `handle_message(bot, message)`, `on_message` осталась тонкой обёрткой с `try/except`. Раньше исключение съедалось представлением vkbottle и оставалось строкой в DEBUG-логе.
  - **Алерт владельцу в личку** (Р8): необработанное исключение → сообщение на `ADMIN_ID`, не чаще раза в 10 минут (`_ALERT_MIN_GAP_SECONDS`). В алерте только беседа, номер сообщения и тип ошибки — текста участника нет (Р7). Сбой доставки самого алерта ничего не ломает. `_last_alert_at` инициализируется `-inf`, а не нулём: `time.monotonic()` на Linux — аптайм, с нуля первые 10 минут после перезагрузки хоста алерты бы молча глушились.
  - **Живая проверка канала, дважды.** Первый вызов вернул `VKAPIError_901 "Can't send messages for users without permission"` — VK не даёт сообществу писать пользователю первым. Пользователь разрешил сообщения от сообщества, повторный вызов прошёл (`message_id = 3`). Записано в DEPLOY.md шагом 8 вместе с командой проверки — без этого действия бот работает как обычно, но о сбоях никто не узнаёт.
  - 158 тестов зелёных (10 новых, новый файл `tests/test_handler_reliability.py` с подставным VK): отказ `users.get` не уничтожает ответ; ретрай отправки; сбой разбора не помечает сообщение обработанным; частичная запись откатывается целиком; успешное сообщение помечается и дедуплицируется; off-topic и команды тоже помечаются; алерт уходит один раз за окно и без текста участника; сбой алерта не ломает обработку; одно плохое сообщение не мешает следующему.
  - **Замечено по ходу, не чинилось**: первая версия теста использовала `monkeypatch.undo()`, который откатил и подмену `_conn` из фикстуры — одна строка успела записаться в локальную dev-базу (`bot.db`, 79 вместо 78 строк). Строка удалена по id с проверкой текста и времени, база вернулась к 78. Тест переписан на явный `setattr` обратно. Это ещё одно проявление A4: боевая база открывается на импорте модуля, и любая ошибка в тесте пишет в неё.
  - **Отдельное наблюдение для будущих этапов**: дискуссионные сообщения («Во всём виноваты те, кто заправляют 95…») попадают в базу как факты о конкретной АЗС. Это не отказ и не в объёме принятых решений, но данные пайплайн засоряет — кандидат в отдельный этап.
  - Не задеплоено на library-vps.
- [x] **Этап 36 — развязка обработки и цикла событий (закрытие H1, H2, H6, F3)** (2026-08-16): четвёртый этап реализации по [ARCH_DECISIONS.md](ARCH_DECISIONS.md), решение Р3.
  - **В поток уходит только вызов провайдера** (`asyncio.to_thread` в `pipeline.py::_analyze`), БД остаётся в цикле событий. `_analyze`/`process_message` стали async, `handle_message` их ждёт. Соединение SQLite привязано к потоку создания (H5), поэтому выносить обработку целиком означало бы второе соединение и явные блокировки взамен утраченной атомарности — а работа с базой занимает миллисекунды, выносить нечего. H1 и H5 таким образом решены вместе: второй просто не наступает.
  - **Живой замер, пять сообщений через Mistral, результат честнее ожидаемого**: параллельно каждый ответ готов через ~2.4-2.8 с, суммарно 2.8 с; последовательно (простой цикл) — 1.0/1.8/3.5/4.3/5.0 с, суммарно 5.0 с. То есть провайдер обслуживает пять одновременных запросов заметно медленнее, чем по одному (~2.5 с против ~0.9 с), и задержка становится не столько меньше, сколько **равномерной**: первый спросивший ждёт дольше, последний — вдвое меньше. При этом «последовательная» колонка — оптимистичная оценка старого поведения: аудит замерял настоящий обработчик, и там все пять ответов уходили в самом конце (5.01, 5.01, 5.01, 5.01, 5.06), потому что цикл событий был занят и отправка тоже ждала. Реально было «все ждут 5 с» → стало «все ждут 2.8 с». Побочно подтверждено, что SDK Mistral корректно работает из нескольких потоков.
  - **Дебаунс — на пару «беседа + автор»** вместо одного окна на чат (H2: замер аудита — 1 ответ из 5 на rule-based, 2 из 5 на Mistral). Поверх — потолок `_CHAT_REPLY_CAP=10` ответов за 60 секунд на беседу, страховка от патологического всплеска; срабатывание пишется в лог, чтобы не быть ещё одним видом молчания (G3). Заодно закрыт F3: публичная `!лимит` больше не съедает окно чужих содержательных ответов.
  - **`reported_at` — из `message.date`** (H6). Живая находка по ходу: `Message.date` у vkbottle — это **уже `datetime` с UTC-таймзоной**, а не unix-таймстамп, как я предполагал по документации VK; первая версия `_message_time` падала на `TypeError`, поймано тестами. Заодно поставлены гварды: naive-значение приводится к UTC (одна naive-строка в БД навсегда ломает ответы по станции — D3), эпоха-ноль и дата из будущего заменяются временем обработки (отчёт «из будущего» никогда не устарел бы).
  - Подставной VK вынесен из `tests/test_handler_reliability.py` в `tests/vk_fakes.py` — его переиспользует новый файл тестов.
  - 166 тестов зелёных (8 новых): всплеск обрабатывается параллельно, а не по очереди; пятеро разных людей получают пять ответов; один и тот же человек по-прежнему дебаунсится; `!лимит` не глушит чужой вопрос; потолок на беседу срабатывает; время факта берётся из сообщения; неправдоподобная дата откатывается ко времени обработки; naive-дата сохраняется с таймзоной и не ломает ответ.
  - Не задеплоено на library-vps (вместе с Этапом 35).
- [x] **Этап 37 — политика отката LLM: отвечаем, но не пишем (закрытие радиуса B1, B2, C3)** (2026-08-16): пятый этап реализации по [ARCH_DECISIONS.md](ARCH_DECISIONS.md), решение Р1.
  - **Асимметрия.** При аварии LLM rule-based продолжает разбирать вопросы, но теряет право писать факты (`pipeline.py::_record`, гвард по `Analysis.llm_failed`, исход `report_suppressed_llm_down`). Цена ошибки разная: неверно извлечённый факт («95 закончился» → «есть», B1) оседает в append-only логе и повторяется в каждом будущем ответе про станцию уже после починки провайдера, а ошибка на вопросе живёт один ответ — текст всё равно собирается из БД шаблоном. Сами B1/B2/C3 при этом **не чинились**: они остаются в коде, но их радиус сведён к ответам на вопросы.
  - **Важная граница: гвард только про аварию.** При `LLM_ENABLED=false` rule-based — штатный путь, право записи у него остаётся. Иначе бот в этом режиме просто перестал бы копить данные, а это не то, что решали. Зафиксировано отдельным тестом.
  - **Второй провайдер перед откатом.** `llm/client.py` переписан на цепочку: основной → `LLM_FALLBACK_PROVIDER` (если задан и отличается) → None. Смысл именно в связке с гвардом: раз резервный путь не пишет факты, каждый сбой основного провайдера без резерва означает потерянные отчёты. **Живая проверка на проде**: Groq через SOCKS5-туннель отвечает за 1.8 с и отдаёт уже новую структуру `queue` — как резервный годится. На проде переменная пока не задана; включать — отдельным решением при деплое.
  - **Колонка `source`** (`llm`/`rule_based`) в `fuel_report`/`station_break`/`fuel_limit`, через тот же догоняющий `ALTER TABLE`. У старых строк `NULL`.
  - 174 теста зелёных (10 новых, два старых переписаны). Переписаны осознанно: `test_falls_back_to_rule_based_when_llm_returns_none` фиксировал ровно то поведение, которое решение Р1 отменяет (отчёт пишется по rule-based при сбое LLM), и `test_burst_is_processed_concurrently...` из Этапа 36, где подставной провайдер возвращал None и теперь упирался в новый гвард. Новые тесты: отчёт не пишется и в БД пусто; вопрос при этом по-прежнему получает ответ; при выключенном LLM запись работает и `source=rule_based`; при живом LLM `source=llm`; цепочка провайдеров (только основной без резерва, резерв не пробуется дважды при совпадении, второй вызывается при отказе первого, не вызывается при успехе, None только после всей цепочки).
  - Не задеплоено на library-vps (вместе с Этапами 35-36).
